# Verified audit — 05-market-data-services

Counts: 42 findings — confirmed 40 | refuted 2 | already-fixed 0
(Refuted: B-02, B-12. No findings already-fixed.)

---

## Bugs

- **B-01 company-data-market-cap-unit-flip** | P1 | **confirmed** | bfinance `Ratios.market_cap` = "₹ Cr" (bfinance/models/company.py:13, parser.py:198 raw screener.in Cr, quotes.py:115 does `*1e7` to absolute) vs yfinance tier maps raw absolute `marketCap` (company_data_service.py:67; live probe `ITC.NS marketCap=3336250064896`); same repo names the Cr form `market_cap_cr` at equity_research_service.py:85; schemas.py:140 exposes bare `market_cap`. | current line: `backend/app/services/company_data_service.py:138` | files to touch: `company_data_service.py`, `app/models/schemas.py`, `tests/test_data_services.py` | regression-test: run `get_fundamentals` once with bfinance-mocked ratios (`market_cap=1900000`) and once with yf-mocked info (`marketCap=1.9e12`); assert both emit the SAME unit/scale (e.g. `market_cap_cr`), fail if magnitudes differ by >1e4.

- **B-02 company-data-dividend-yield-unit-flip** | P1 | **refuted** | Unit claim "yfinance dividendYield is a fraction (0.0085)" is false for installed yfinance 1.7.0: live read-only probe returns percent-scale values (`AAPL dy=0.32` ≈ 0.32%, `ITC.NS dy=6.1`, `RELIANCE.NS dy=0.49` — fraction reading would imply 32–610% yields); yfinance passes Yahoo raw through with no conversion (yfinance/scrapers/quote.py:724-737) and bfinance is percent too ("Dividend Yield %", models/company.py:19) → both tiers now emit percent, no flip. | current line: `backend/app/services/company_data_service.py:145` | files to touch: none (audit fix not needed; optionally a unit-contract test) | regression-test: pin installed-yfinance contract — assert bfinance-tier and yf-tier `dividend_yield` for same ticker differ by <5× (catches a future Yahoo/yfinance unit regression).

- **B-03 company-data-roe-unit-flip** | P1 | **confirmed** | bfinance `r.roe` is percent (screener.in raw "ROE", models/company.py:21, fixture 14.5 at test_equity_research_and_screens.py:69) vs yfinance `returnOnEquity` is ratio (live probe `AAPL roe=1.4875` = 148.75%); bfinance itself divides `/100.0` "to match yfinance" at bfinance/market/quotes.py:244, screener_service.py:292 does `*100`. | current line: `backend/app/services/company_data_service.py:148` | files to touch: `company_data_service.py`, `tests/test_data_services.py` | regression-test: cross-tier contract — bfinance-mocked ratios (`roe=14.5`) and yf-mocked info (`returnOnEquity=0.145`) must produce the same `return_on_equity` value for the same economic ROE.

- **B-04 av-history-outputsize-missing** | P1 | **confirmed** | `fetch_daily_ohlcv` calls `_make_request("TIME_SERIES_DAILY", {"symbol": av_symbol})` with no `outputsize` → AV default `compact` (~100 sessions); caller requests `dl_start = _deep_start` (data_service.py:361, `DEEP_CACHE_YEARS=10` at :121) and stores any non-empty partial frame as success (data_service.py:798-813); test mocks `_make_request` with a pre-built series (test_coverage_alpha_vantage.py:166) so the param gap is untested (grep: no `outputsize` in backend/tests). | current line: `backend/app/services/alpha_vantage_service.py:263` | files to touch: `alpha_vantage_service.py`, `tests/test_alpha_vantage.py` or `tests/test_coverage_alpha_vantage.py` | regression-test: mock `requests.get`, call `fetch_daily_ohlcv`, assert recorded query params include `outputsize=full`.

- **B-05 equity-research-valueerror-maps-to-404** | P2 | **confirmed** | `except Exception … raise ValueError` at equity_research_service.py:121 (also :186, :249, :283) and api/equity_research.py:46-47 maps `ValueError`→404, so bfinance outage = "not found"; correct convention already present: company_data_service.py:177 raises RuntimeError (docstring :107-108); ai_dossier_service.py:50,70,86,102 raise ValueError but their routes catch `Exception`→500 (api:134-136 etc.). | current line: `backend/app/services/equity_research_service.py:121` | files to touch: `equity_research_service.py`, `api/equity_research.py`, `tests/test_equity_research_and_screens.py` | regression-test: patch `bf.Ticker._ensure_profile` to raise a network-ish error; GET `/company/{t}/full-profile` must return 503 (not 404); keep 404 for genuine no-profile.

- **B-06 screener-valueerror-maps-to-400** | P2 | **confirmed** | `raise ValueError(f"Screen execution failed: {e}")` at screener_service.py:252; route maps ValueError→400 at api/equity_research.py:205-206 blaming client for vendor failure, while custom-screen route turns same class into 500 (api:227-229); ValueError for "Unknown strategy" at screener_service.py:187 is the only legitimate 400. | current line: `backend/app/services/screener_service.py:252` | files to touch: `screener_service.py`, `api/equity_research.py`, `tests/test_p09_screener.py` | regression-test: make `screen_getter().run` raise; GET `/screens/coffee_can` must be 503/500, while unknown strategy stays 400.

- **B-07 currency-hardcoded-fallback-cached** | P2 | **confirmed** | `rate = live_usd_inr if … else 83.0` (currency_service.py:188) is stored under the normal cache key with `_last_updated` set (:55-57), so `get_exchange_rate_info()['usd_to_inr']` (:142) and `convert_amount` serve 83.0 as live for 30 min; test only asserts the fallback returns 83.0, not that it is/isn't cached (test_coverage_services_and_api.py:84-89). | current line: `backend/app/services/currency_service.py:188` | files to touch: `currency_service.py`, `tests/test_coverage_services_and_api.py` | regression-test: patch yfinance to fail, call `get_exchange_rate`, then assert either cache NOT updated or info payload marks `source="fallback"`; next successful fetch must overwrite.

- **B-08 av-dot-bo-not-bridged** | P2 | **confirmed** | `to_av_symbol` maps only `.NS`→`.BSE` (alpha_vantage_service.py:52-53) while docstring :47-48 says AV's BSE feed is `.BSE` yet claims ".BO already matches AV convention" (it's yfinance's suffix) — `.BO` tickers pass through (:54) and fail the AV tier; behavior locked by test_alpha_vantage.py:63 and test_coverage_alpha_vantage.py:34 (`TCS.BO == "TCS.BO"`). | current line: `backend/app/services/alpha_vantage_service.py:54` | files to touch: `alpha_vantage_service.py`, `tests/test_alpha_vantage.py`, `tests/test_coverage_alpha_vantage.py` | regression-test: `to_av_symbol("500112.BO") == "500112.BSE"` (or explicit documented skip returning None) plus a fetch-tier test for a `.BO` ticker.

- **B-09 company-data-to-thread-drops-args** | P2 | **confirmed** | `async def _to_thread(func, *args): return await asyncio.to_thread(func)` drops `*args` (company_data_service.py:54-55); siblings forward correctly (screener_service.py:55-56, equity_research_service.py:25-26, ai_dossier_service.py:22-23); current callers pass only the callable (:173,:213,:278,:290,:332) so it's latent, no test covers args. | current line: `backend/app/services/company_data_service.py:55` | files to touch: `company_data_service.py` (one line), `tests/test_coverage_alpha_vantage.py` | regression-test: `asyncio.run(_to_thread(lambda a, b: a + b, 1, 2)) == 3`.

- **B-10 av-quote-returns-bridged-ticker** | P2 | **confirmed** | Quote dict returns `"ticker": av_symbol.upper()` (alpha_vantage_service.py:310) so `RELIANCE.NS`→`RELIANCE.BSE`; OHLCV path is re-normalized (data_service.py:884 `df['ticker'] = ticker.upper()`) but `_fallback_quote` returns the AV dict unmodified (data_service.py:825-841 only `setdefault is_indian`). | current line: `backend/app/services/alpha_vantage_service.py:310` | files to touch: `alpha_vantage_service.py`, `tests/test_alpha_vantage.py` | regression-test: `fetch_global_quote("RELIANCE.NS")` returns `"ticker": "RELIANCE.NS"` (requested), while `currency`/`exchange` still reflect BSE.

- **B-11 equity-research-concalls-swallow-errors** | P2 | **confirmed** | `except Exception … return []` at equity_research_service.py:203-205 turns outage into HTTP 200 `{"count": 0}` (api/equity_research.py:75-76), indistinguishable from no-data; sibling methods raise (:121, :186, :249, :283); no error-path test (test_equity_research_and_screens.py:169-174, :271-279 only happy path). | current line: `backend/app/services/equity_research_service.py:205` | files to touch: `equity_research_service.py`, `tests/test_equity_research_and_screens.py` | regression-test: `_ensure_profile` raises → `get_concalls` must raise (route 5xx), while `profile.concalls == []` still returns `[]`/count 0.

- **B-12 equity-research-roe-fallback-mixed-units** | P2 | **refuted** | Mechanism unreachable: fallback `info.get("returnOnEquity")` is bfinance's own info (built at ticker.py:143 via QuoteEngine.build_info_dict) where quotes.py:244 sets `"returnOnEquity": (r.roe / 100.0) if r.roe else None` — a None-preserving derivation from the SAME `r.roe`; when `r.roe is None` the fallback is always `None`, never `0.145`. Response can only be `r.roe` (%) or None. Fixture split (:42 vs :69) never exercises the fallback because mock `roe=14.5`. | current line: `backend/app/services/equity_research_service.py:92` | files to touch: none required | regression-test (optional hardening): profile with `roe=None` and bfinance-built info → assert `profile["roe"] is None` (guards against a future bfinance info that populates `returnOnEquity` independently).

- **B-13 screener-div-yield-filter-unit** | P2 | **confirmed** | `div_yield = (info.get("dividendYield") or 0.0) * 100` (screener_service.py:295) reads bfinance info where quotes.py:241 sets `dividendYield = r.dividend_yield` RAW percent (models/company.py:19 "Dividend Yield %", parser.py:205 no /100, bfinance ai/context.py:75 prints `{…:.2f}%`); percent×100 admits nearly every payer vs `min_div_yield=2.5`, while equity_research_service.py:90 renders the same quantity as-is (percent); bfinance internally contradictory (screens.py:115/216 treat it as fraction); no test sets `min_div_yield` (grep over tests: only fixture `market_cap_cr` matches). | current line: `backend/app/services/screener_service.py:295` | files to touch: `screener_service.py`, `tests/test_p09_screener.py` | regression-test: fake `bf.Ticker.info = {"dividendYield": 0.85}` (bfinance percent), call `run_custom_screen(min_div_yield=2.5)` → stock must be EXCLUDED; with `2.81` → included.

- **B-14 india-data-unguarded-next-close-col** | P3 | **confirmed** | Guard at india_data_service.py:245 checks only `volume`; line 247 `next(c for c in df.columns if c.lower() in ["close", "adj_close"])` raises StopIteration→RuntimeError inside the coroutine for a volume-only frame; untested (test_coverage_india_data.py:105-108 always supplies `close`). | current line: `backend/app/services/india_data_service.py:247` | files to touch: `india_data_service.py`, `tests/test_coverage_india_data.py` | regression-test: `calculate_portfolio_liquidity_limits` with a df containing only `volume` → returns defaults (adv fallback), no exception.

- **B-15 india-data-none-ohlc-typeerror** | P3 | **confirmed** | `float(rec.get("open", 0.0))` / `int(rec.get("ttl_trd_qnty", 0))` raise TypeError when key exists with value `None` (india_data_service.py:84-92) while `deliv_*` at :93-94 explicitly guard None; only test callers exist (test_coverage_india_data.py:51-64 always well-formed) so latent. | current line: `backend/app/services/india_data_service.py:84` | files to touch: `india_data_service.py`, `tests/test_coverage_india_data.py` | regression-test: record `{"symbol":"X","open":None,"ttl_trd_qnty":None}` → ingests (0.0/0) instead of TypeError.

- **B-16 av-premium-classified-as-daily** | P3 | **confirmed** | `if "premium" in low: return "daily"` (alpha_vantage_service.py:82-83) retires the key until local midnight on any upsell notice; locked in by test_coverage_alpha_vantage.py:42 (`"Please upgrade to premium" == "daily"`). | current line: `backend/app/services/alpha_vantage_service.py:83` | files to touch: `alpha_vantage_service.py`, `tests/test_coverage_alpha_vantage.py` | regression-test: classify `"Explore our premium endpoints" ` → not `daily` (should be `other`/`frequency` policy decision), while genuine daily-limit phrasing still → `daily`.

- **B-17 av-retirement-uses-local-date** | P3 | **confirmed** | `if self.retired_on == _date.today(): return False` (alpha_vantage_service.py:108) keys retirement to host-local date while AV quota resets at US midnight — IST host keeps key retired ~10h past reset (self-correcting worst case = one wasted call). | current line: `backend/app/services/alpha_vantage_service.py:108` | files to touch: `alpha_vantage_service.py` | regression-test: freeze clock across a US-midnight-but-not-IST-midnight boundary → `available()` becomes True (requires injecting tz/date source).

- **B-18 av-raise-for-status-outside-classifier** | P3 | **confirmed** | `response.raise_for_status()` at alpha_vantage_service.py:226 runs before notice classification (:233-249), so HTTP 429/5xx aborts the whole key-rotation loop with raw `requests.HTTPError` instead of demoting the current key; currently only absorbed by the broad catch at data_service.py:794-796 (and :395 for the main path). | current line: `backend/app/services/alpha_vantage_service.py:226` | files to touch: `alpha_vantage_service.py`, `tests/test_alpha_vantage.py` | regression-test: first key returns HTTP 500, second returns 200 payload → `_make_request` succeeds via rotation (records 2 calls), first key cooldown/retired.

- **B-19 currency-builtin-any-annotation** | P3 | **confirmed** | `def get_exchange_rate_info(self) -> Dict[str, any]:` uses builtin `any` not `typing.Any` (currency_service_service.py:131 — exact: currency_service.py:131); harmless at runtime, wrong type. | current line: `backend/app/services/currency_service.py:131` | files to touch: `currency_service.py` | regression-test: `typing.get_type_hints(CurrencyConversionService.get_exchange_rate_info)` return annotation is `Dict[str, Any]`.

## Improvements

- **I-01 production-code-mock-detection** | P2 | **confirmed** | `import unittest.mock` + `isinstance(yf.Ticker, Mock/MagicMock)` inside get_fundamentals (company_data_service.py:111-117, skip at :211-212) and get_financial_statements (:255-261); same pattern at data_service.py:540-548 and :733-739; a test's mock changes real vendor-cascade control flow (comment at test_source_preference_and_cache.py:160-162 acknowledges the hazard). | current line: `backend/app/services/company_data_service.py:117` | files to touch: `company_data_service.py`, `data_service.py`, test conftest (env-flag seam), affected tests | regression-test: with `yf.Ticker` a real class (unmocked) and a stub env flag off, bfinance tier is attempted — i.e. cascade order independent of mock object identity.

- **I-02 unknown-vendor-falls-to-yfinance** | P2 | **confirmed** | cascade is `if vendor == "bfinance": … else: yf` (company_data_service.py:209-218) so any typo/unknown tier silently fetches yfinance instead of failing fast; `source_preference_service.validate_source` (:33-40) already defines the known set. | current line: `backend/app/services/company_data_service.py:216` | files to touch: `company_data_service.py` | regression-test: `get_fundamentals(t, source_order=["bfincance"])` raises ValueError naming the bad vendor (no vendor call attempted).

- **I-03 screener-l1-unbounded-cache** | P3 | **confirmed** | class-level `_cache` keyed `f"{strat}_{max}_{universe_token}"` (screener_service.py:62, :194) never evicts: expired entries linger until the identical key is recomputed (:196-199 only checks TTL on read); distinct custom universes grow it unboundedly. | current line: `backend/app/services/screener_service.py:62` | files to touch: `screener_service.py`, `tests/test_screener_cache.py` | regression-test: insert 1000 distinct-universe keys with old timestamps, trigger one run → `len(ScreenerService._cache)` drops to ≤ small cap.

- **I-04 screener-row-mapping-duplicated** | P3 | **confirmed** | DataFrame→dict block copy-pasted: run_screen :216-229 (includes `book_value`) vs run_custom_screen :319-331 (omits it); same keys otherwise. | current line: `backend/app/services/screener_service.py:319` | files to touch: `screener_service.py`, `tests/test_p09_screener.py` | regression-test: existing p09 tests keep passing after extracting `_row_to_stock`; add assertion both paths emit identical key sets minus documented `book_value` difference (or unify).

- **I-05 screener-async-without-io** | P3 | **confirmed** | `async def get_available_strategies` (screener_service.py:101-110) does no await/I/O — pure list build; nit, kept only for await-consistency with the route. | current line: `backend/app/services/screener_service.py:101` | files to touch: `screener_service.py`, `api/equity_research.py:179` | regression-test: n/a beyond existing `test_api_screens_list` / `test_get_available_strategies` (update awaits if def changes).

- **I-06 screener-singleton-first-call-wins** | P3 | **confirmed** | `get_screener_service` ignores later `db_session`/`cache_service` args once singleton exists (screener_service.py:356-358); that's why the run_screen route builds a fresh instance per request (api/equity_research.py:196-203) while `/screens` (:178) and `/screens/custom` (:218) use the singleton. | current line: `backend/app/services/screener_service.py:357` | files to touch: `screener_service.py` (or delete singleton), `api/equity_research.py` | regression-test: call `get_screener_service(cache_service=A)` then `get_screener_service(cache_service=B)` → raises (or returns B), never silently A.

- **I-07 india-participation-rates-unused-mutable-default** | P3 | **confirmed** | `participation_rates: List[float] = [0.10, 0.20]` (india_data_service.py:219) is never read — :262-263 hardcode `0.10`/`0.20`; mutable default as well. | current line: `backend/app/services/india_data_service.py:219` | files to touch: `india_data_service.py` | regression-test: pass `participation_rates=[0.05]` → `days_to_liquidate_*` keys reflect 0.05 (or param removed and call sites updated).

- **I-08 india-ingest-no-production-caller** | P3 | **confirmed** | grep of `backend/app` finds only the definitions (india_data_service.py:61, :104); callers are tests only (test_coverage_india_data.py:68, :76, :87); readers `/analytics/india-flows` and `/delivery-anomalies` exist (api/analytics.py:1996, :2018) and would read empty tables in prod; CONTEXT.md:210 shows ingestion cron **t28 = deferred**. | current line: `backend/app/services/india_data_service.py:61` | files to touch: `CONTEXT.md` / issue tracker (track gap), optionally `api/analytics.py` empty-state note | regression-test: n/a (process gap) — instead an endpoint test asserting an explicit `"ingestion": "unavailable"`/empty-state disclaimer, or ticket linked to t28.

- **I-09 currency-format-dup-and-noop-contextmanager** | P3 | **confirmed** | `format_currency_indian` (currency_service.py:103-125) reimplements `format_currency` (:79-101) plus a `K` tier; `CurrencyServiceContext.__aexit__` is `pass` (:222-224) and adds nothing over `get_currency_service()`. | current line: `backend/app/services/currency_service.py:103` | files to touch: `currency_service.py` | regression-test: existing format assertions (test_coverage_services_and_api.py:94-98) keep passing after collapse to one formatter + plain singleton.

- **I-10 ai-dossier-format-param-unvalidated** | P3 | **confirmed** | `format: Literal["markdown","json"]` (ai_dossier_service.py:35) shadows builtin and is never runtime-checked; route passes raw query string (`api/equity_research.py:156`, forwarded :163) so `format=xml` reaches `t.to_ai_context(format=…)` (service :44) before failing; no validation test. | current line: `backend/app/services/ai_dossier_service.py:35` | files to touch: `ai_dossier_service.py`, `api/equity_research.py`, `tests/test_equity_research_and_screens.py` | regression-test: `get_ai_dossier(t, format="xml")` raises ValueError immediately (no `bf.Ticker` construction — assert mock not called); route returns 4xx.

- **I-11 normalize-to-thread-shims-redeclared** | P3 | **confirmed** | `_normalize` redeclared in equity_research_service.py:20-22, company_data_service.py:31-33, ai_dossier_service.py:17-19; `_to_thread` variants in equity_research (:25-26), ai_dossier (:22-23), screener (:55-56) plus the broken company_data (:54-55) — four copies, one already diverges (B-09). | current line: `backend/app/services/equity_research_service.py:20` | files to touch: new shared util (e.g. `app/services/_async.py`), the 4 service files | regression-test: existing suite (identity of behavior) — plus unit test that shared `_to_thread` forwards args (covers B-09 permanently).

- **I-12 av-quote-payload-inconsistencies** | P3 | **confirmed** | `"change_percent": q.get("10. change percent")` is the raw `"1.4%"`-style string and only AV produces the key (app-grep: sole producer alpha_vantage_service.py:321; tests only echo it in fixtures); `"timestamp": pd.Timestamp.utcnow()` (:325) is UTC while bfinance/yfinance quote timestamps are `Asia/Kolkata` (data_service.py:463, :536). | current line: `backend/app/services/alpha_vantage_service.py:321` | files to touch: `alpha_vantage_service.py`, `data_service.py` consumers | regression-test: `fetch_global_quote` output has numeric `change_percent` (or key documented AV-only) and `timestamp` parses to IST offset (+05:30) like other tiers.

- **I-13 empty-init-and-useless-singletons** | P3 | **confirmed** | `__init__: pass` at equity_research_service.py:32-33 and ai_dossier_service.py:29-30; process-wide singletons (:286-293, :105-112) hold no config/cache — indirection buys nothing (equity route constructs via getter at api:43, ai at api:131). | current line: `backend/app/services/equity_research_service.py:33` | files to touch: `equity_research_service.py`, `ai_dossier_service.py`, their API routes | regression-test: existing API tests unchanged after replacing singleton with module-level instance.

- **I-14 india-zero-variance-std-substitute** | P3 | **confirmed** | `std_deliv = float(np.std(historical_deliv)) or 1.0` (india_data_service.py:197) substitutes 1.0 percentage-point whenever history is flat, so a +2pp bump is "anomaly" and +1.9pp isn't — arbitrary scale; test always has variance 25→50 (test_coverage_india_data.py:87-100). | current line: `backend/app/services/india_data_service.py:197` | files to touch: `india_data_service.py` | regression-test: flat history (`deliv_per` all 40.0) + current 42.0 → flag `z_score is None`/`is_anomaly False` with explicit reason, not z=2.0.

## Optimizations

- **O-01 yfundamentals-tier-uncached** | P2 | **confirmed** | `_fetch_yf` re-calls `t.info` with up to 3 rate-limit retries + sleeps (company_data_service.py:36-51, :167-177) on EVERY fundamentals request — no memo/TTL anywhere in the file; bfinance tier is covered by `SQLiteCache(default_ttl_hours=24)` (bfinance/ticker.py:45). | current line: `backend/app/services/company_data_service.py:170` | files to touch: `company_data_service.py` (or `cache_service.py`), `tests/test_data_services.py` | regression-test: two `get_fundamentals` calls (yf-mocked) → underlying `Ticker.info` property hit once within TTL.

- **O-02 debt-free-sequential-leverage-fanout** | P2 | **confirmed** | `_check` loops `bf.Ticker(r["symbol"]).info` one-by-one inside a single worker thread (screener_service.py:256-266, loop :258-265) — N serial network fetches on cold `debt_free` runs, no separate timeout budget; warm runs lean only on bfinance's 24h cache. | current line: `backend/app/services/screener_service.py:260` | files to touch: `screener_service.py`, `tests/test_p09_screener.py` | regression-test: fake Ticker.info with a barrier/counter — N candidates → ≤K concurrent inits (bounded pool) and identical kept-set vs serial order.

- **O-03 run-custom-screen-has-no-cache** | P3 | **confirmed** | `run_custom_screen` never touches L1 `_cache` (:62/:196-199) nor L2 `_get/_set_cached_screen` (:112-174); it always runs `bf.Screen.run` (screener_service.py:310-335); sibling `run_screen` caches. | current line: `backend/app/services/screener_service.py:335` | files to touch: `screener_service.py`, `tests/test_p09_screener.py` | regression-test: two identical custom-screen calls → second served from L1 (upstream `Screen.run` called once).

- **O-04 fresh-bf-ticker-per-method** | P3 | **confirmed** | each method builds `bf.Ticker(norm_ticker)` fresh (equity_research_service.py:43, :131, :195, :215, :258); dashboard profile+shareholding+concalls+ratios rebuilds the profile 4×; masked by bfinance's 24h SQLite cache — audit itself says "do not pre-build". | current line: `backend/app/services/equity_research_service.py:43` | files to touch: none now (defer until profiling shows cache misses) | regression-test: n/a now; future change: one dashboard load → `Ticker._ensure_profile` invoked ≤1 time per ticker per request scope.

## Verified clean (audit claims re-checked)

- **VC-01 source-preference-clean** | — | **confirmed** | validation strict (source_preference_service.py:33-40), read fail-safe default (:59-61), upsert+`expire_all` (:67-76), `source_order_for` (:81-85) matches tests (test_source_preference_and_cache.py:148-149); unknown-pair FX rejection covered (test_contract_p1_batch.py:294-300). | current line: `backend/app/services/source_preference_service.py:81` | files to touch: none | regression-test: existing tests suffice.

- **VC-02 av-budget-rotation-clean** | — | **confirmed** | local budget guard blocks without network (alpha_vantage_service.py:215-218, test_alpha_vantage.py:116-124 asserts 1 recorded call), day rollover/cooldown/retire tested (test_coverage_alpha_vantage.py:45-70), rate-limit-before-key taxonomy ordering at :76-81 with tests :39-43. | current line: `backend/app/services/alpha_vantage_service.py:215` | files to touch: none | regression-test: existing set suffices.

- **VC-03 screener-run-screen-cache-clean** | — | **confirmed** | universe isolation, max_stocks caps results-not-universe, numeric-BSE mapping, debt enforcement all covered (test_p09_screener.py:42-107); L2 semantics covered (test_screener_cache.py:91-145 incl. column-size guard). | current line: `backend/app/services/screener_service.py:193` | files to touch: none | regression-test: existing tests suffice.

- **VC-04 india-math-primitives-clean** | — | **confirmed** | Amihud/days-to-liquidate + zero-guards tested (test_coverage_india_data.py:26-44); liquidity tier HIGHLY_LIQUID asserted (:110-116). | current line: `backend/app/services/india_data_service.py:25` | files to touch: none | regression-test: existing tests suffice.

- **VC-05 optimization-axis-clean-claims** | — | **confirmed** | every cited external call offloaded via `asyncio.to_thread` (alpha_vantage:225, screener:233/269/335, equity_research:118/183/202/246/280, company_data:173/278/290/332, currency:183, ai_dossier:47/67/83/99 — all re-read and present); AV timeout `config.py:32` used at alpha_vantage_service.py:223; currency double-checked `asyncio.Lock` + 30-min TTL at currency_service.py:42-57; API keys logged last-4 only (:153, :157, :160) and never embedded in raised errors (errors carry AV notice strings only). | current line: `backend/app/services/alpha_vantage_service.py:225` | files to touch: none | regression-test: existing suite + a log-capture test asserting full API keys never appear in log output.
