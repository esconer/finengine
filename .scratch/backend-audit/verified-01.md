# Verified: 01-api-analytics.md

Verification pass (read-only; no code edited). Source re-read in full: `backend/app/api/analytics.py` (2202 lines, line numbers match report exactly). Empirical probes run from `backend/` via `uv run python -c ...`.

**Totals: 32 confirmed | 0 refuted | 0 already-fixed**

(Bugs 15/15 confirmed · Improvements 11/11 confirmed · Optimizations 6/6 confirmed)

## Bugs

| id/short-name | severity | verdict | evidence (1 line) | current line | files to touch | regression-test idea |
|---|---|---|---|---|---|---|
| `_q` NaN passthrough | P1 | confirmed | `return round(float(val), 6)` at line 97; probe: `_q(lambda: float('nan'))` returns `nan` (not None), and `JSONResponse.render` uses `allow_nan=False` → 500 | 97 | backend/app/api/analytics.py; backend/tests/test_coverage_analytics_extended.py | In `test_coverage_analytics_extended.py::test_q_metric_guard`, add `assert _q(lambda: float('nan')) is None` and `assert _q(lambda: float('inf')) is None` |
| single-holding optimize fabricates metrics | P1 | confirmed | `"expected_annual_return": 0.12,` / `"expected_annual_volatility": 0.22,` / `"expected_sharpe": 0.45` hardcoded in the `len(ticker_list) == 1` branch | 1655-1657 | backend/app/api/analytics.py; backend/tests/ (new endpoint test) | New test in `backend/tests/test_coverage_analytics_all_routes.py`: POST `/analytics/optimize/run` with a single-ticker universe; assert response has no fabricated `expected_annual_return`/`expected_sharpe` (None or computed from real returns), weights `{T: 1.0}` |
| factor-exposure error placeholder `{alpha:0, market:1}` | P1 | confirmed | Lines 609-610 and 628-629 return `"alpha": 0.0, "market": 1.0` on error; CONTEXT.md:283 gotcha 23 forbids it; `tests/test_wave2_transition.py:111` even enshrines `fe_resp.json()["portfolio"]["market"] == 1.0` | 609-610, 628-629 | backend/app/api/analytics.py; backend/tests/test_wave2_transition.py | In `test_wave2_transition.py` empty-state block, change assertion to `portfolio["market"] is None and "error" in resp.json()` |
| benchmark rebase off-by-one | P2 | confirmed | `cum_bench = (1.0 + bench_ret.loc[common_dates]).cumprod()` seeds first point at `initial_val*(1+r_d0)` while `initial_val = portfolio_series.loc[common_dates[0]]` is the portfolio value on that same date (probe: 101.0 vs 102.01 at common[0]) | 1259 | backend/app/api/analytics.py | Unit test on `get_performance_history`: with stubbed portfolio series and benchmark returns, assert `benchmark_value == portfolio_value` on the first common date |
| /concentration mixed weight sources | P2 | confirmed | Metrics use MV weights from `_load_portfolio_allocation` (line 704) but `total_position_weight = sum(pos.weight or 0.0 …)` (727) normalizes `by_sector` off the stored `weight` column | 727 | backend/app/api/analytics.py | Test in `test_api_endpoints.py`: seed 2 positions with stale `weight` but real `market_value`; assert `sum(by_sector.values()) ≈ 1.0` and sector shares match MV weights, not stored weights |
| `tickers=portfolio` sentinel undocumented-in-code | P2 | confirmed | Line 200 `description="Comma-separated tickers or 'portfolio'"` but `resolve_allocation` (67-88) has no `portfolio` case → becomes ticker `"PORTFOLIO"` → equal-weight fallback (83-84). (Report's "877" is a typo for 1877; claims at 1937/2020/2058/2109/2153 all exact; no frontend/test caller exists) | 200 | backend/app/api/analytics.py | Test hitting `/analytics/realized-risk?tickers=portfolio` with a seeded DB portfolio: assert response equals the `tickers=` omitted response (portfolio metrics, not a `"PORTFOLIO"` fetch error) |
| `_TAILS_RESPONSE_CACHE` never pruned | P2 | confirmed | Line 2193 writes `_TAILS_RESPONSE_CACHE[cache_key] = ...` with no eviction anywhere; grep shows the symbol only in analytics.py; `data.py:254` clear → `clear_market_data_cache` (cache_service.py:211) never touches it | 2193 | backend/app/api/analytics.py; backend/app/api/data.py (or cache_service.py) | New `backend/tests/test_tails_cache.py`: call `/analytics/tails` twice with different `lookback_days`, assert expired entry count is bounded / a clear hook empties `_TAILS_RESPONSE_CACHE` |
| tear-sheet benchmark fetch not best-effort | P2 | confirmed | Line 1398 `bench_ret = await benchmark.get_returns(...)` sits outside any try, unlike the guarded pattern at 653-656 and 1048-1051; exception falls to outer handler at 1529 → HTTP 500 for the whole sheet | 1398 | backend/app/api/analytics.py | Test monkeypatching `benchmark.get_returns` to raise; GET `/analytics/tear-sheet` must still return 200 with `metrics` populated and empty `relative_vs_nifty` |
| fabricated metric defaults | P2 | confirmed | `"var_95": metrics.get("var_95", -0.032)` (346), `cvar_95 -0.047` (347), `hit_ratio 0.5` (348), `annual_volatility 0.20` (340), position copies (395-398), `risk_result.get("overall_score", 25.0)` (1160), `herfindahl_index, 0.25` (1163) | 340-348, 395-398, 1160, 1163 | backend/app/api/analytics.py | Test stubbing `analytics_engine.calculate_portfolio_metrics` to return `{}`; GET `/analytics/realized-risk` and assert `portfolio["var_95"] is None` (no -0.032) and `hit_ratio is None` |
| /summary risk score without benchmark | P2 | confirmed | Line 1134 `risk_scoring(price_data, weights)` omits `benchmark_data` while /risk-score passes it at 1054; engine (analytics_engine.py:750-757, 775-777) excludes + renormalizes the factor leg when benchmark is None → different scores | 1134 | backend/app/api/analytics.py | Test: with seeded positions and stubbed benchmark service, GET `/analytics/summary` and `/analytics/risk-score`; assert `summary["risk_score"] == risk_score["overall_score"]` |
| ad-hoc /liquidity-limits fabricates portfolio_value | P2 | confirmed | Line 2070 builds `PortfolioPosition(..., market_value=10000.0)` per ad-hoc ticker; india_data_service.py:226-229 sums `market_value` and 293 returns it as `portfolio_value` with no ad-hoc flag | 2070 | backend/app/api/analytics.py | Test: GET `/analytics/liquidity-limits?tickers=A.NS,B.NS`; assert response contains an explicit ad-hoc marker (or omits `portfolio_value`), and does not report fake ₹10,000-based totals as real |
| dead `wiped` else-branch | P3 | confirmed | `wiped = sorted(set(price_data_dict) - set(masked_dict))` (265) makes every wiped ticker ∈ `price_data_dict` by construction (holding_window, holdings.py:123-141, only outputs keys from its input dict) → `else` at 426-431 unreachable | 426-431 | backend/app/api/analytics.py | Trivial: static/dead-code removal; regression = existing `realized-risk` warning-shape test still passes with the single surviving branch |
| liquidity inline price-column extraction | P3 | confirmed | Line 816 `price_col = 'adj_close' if 'adj_close' in df.columns else 'close'` misses `Adj Close`/`Close` accepted by `_price_series` (112-114); DataService renames to lowercase today (data_service.py:873-879) so it is latent, as reported | 816 | backend/app/api/analytics.py | Unit test feeding a TitleCase frame (`Adj Close`/`Close` columns) through the liquidity extraction path; assert ticker is included in `price_data_dict` |
| performance-history substitutes qty=1.0 | P3 | confirmed | Line 1208 `quantities[t] = q if q > 0 else 1.0` silently fabricates a share for degenerate rows (no market_value/last_price to derive q at 1206) | 1208 | backend/app/api/analytics.py | Test seeding a position with `quantity=0, market_value=None, last_price=None`; GET `/analytics/performance-history`; assert that ticker is excluded (or flagged) rather than contributing price×1.0 |
| `datetime.utcnow()` deprecated | P3 | confirmed | `datetime.utcnow().isoformat()` at 1088, 1118, 1164; probe raises `DeprecationWarning: datetime.datetime.utcnow() is deprecated` on the project's Python (requires-python = ">=3.12"); mixed with local `datetime.now()` windows at 1093-1094 | 1088 (also 1118, 1164) | backend/app/api/analytics.py | Test with `warnings.simplefilter("error")` calling `/analytics/summary`; assert no DeprecationWarning and `last_updated` parses as timezone-aware UTC |

## Improvements

| id/short-name | severity* | verdict | evidence (1 line) | current line | files to touch | regression-test idea |
|---|---|---|---|---|---|---|
| mixed error contract (200+error vs 404/500) | improvement (QH-04) | confirmed | Line 222 returns HTTP 200 + `"error"` on resolve failure while tear-sheet/optimize/vol-cone raise 404/400 at 1366/1648/2129 | 222 | backend/app/api/analytics.py (client contract; track under QH-04) | Contract test: for each analytics route with an empty DB, assert a consistent status-code policy (e.g., all resolve-failures are 404) once the migration lands |
| unused `cache_service`/`data_service` Depends | improvement | confirmed | `cache_service` declared at 205 and 455, `data_service` at 696 — none referenced anywhere in their handler bodies | 205, 455, 696 | backend/app/api/analytics.py | Import/signature introspection test: assert `get_realized_risk` signature has no `cache_service` param after cleanup (or simply rely on ruff F841-style lint if widened) |
| forecast-risk hardcoded window | improvement | confirmed | Lines 481-482 hardcode `end`/`start = now-252d` while sibling `get_realized_risk` declares `start`/`end` Query params at 201-202 | 481 | backend/app/api/analytics.py | Test GET `/analytics/forecast-risk?start=2025-01-01&end=2025-06-01`; assert response `data_range` echoes the requested window |
| untyped request bodies | improvement | confirmed | `body: Dict = Body(default={})` at 1625 and 1707, `Body(...)` at 1803, with hand-rolled coercions at 1638-1641/1716-1720/1822-1823; `StressTestRequest` pattern already imported at line 25 | 1625, 1707, 1803 | backend/app/api/analytics.py; backend/app/models/schemas.py | Test POSTing invalid `body` (e.g. `strategy: 123`) to `/optimize/run` and assert FastAPI 422 validation error after Pydantic model adoption |
| `import quantstats` inside handler | improvement | confirmed | Line 1355 `import quantstats as qs` sits inside `get_tear_sheet` while every other import is module-level (lines 5-38) | 1355 | backend/app/api/analytics.py | No behavioral test needed; verify `import app.api.analytics` exposes `qs` (or module-level import succeeds) in a unit test |
| missing blank lines before `/india-flows` | improvement | confirmed | Line 1995 `raise HTTPException(...)` then line 1996 immediately `@router.get("/india-flows")` with zero blank lines | 1996 | backend/app/api/analytics.py | Formatting-only; covered by any repo formatter/lint pass — no regression test required |
| `getattr(pos, "added_on", None)` on real column | improvement | confirmed | `getattr(pos, "added_on", None)` at 1234 and 1338, but `added_on` is a real `PortfolioPosition` column (database.py:29) — getattr hides typos | 1234, 1338 | backend/app/api/analytics.py | Existing performance-history tests still pass with plain `pos.added_on`; add assertion that `history_coverage`/holdings window reflects seeded `added_on` date |
| domain ValueError → HTTP 409 | improvement | confirmed | Line 1795 `raise HTTPException(status_code=409, detail=str(e))` for regime ValueError; rest of file uses 400/404 (1698, 1929, 2136) | 1795 | backend/app/api/analytics.py | Test: monkeypatch `detect_regime` to raise ValueError; GET `/analytics/regime`; assert status is 400/422 (whichever convention is adopted) |
| dead coroutine/hasattr branches | improvement | confirmed | Lines 787 and 793 (`asyncio.iscoroutine(res)`, `hasattr(data_service, 'fetch_quote')`) always true: `fetch_historical_data`/`fetch_quote` are `async def` at data_service.py:307/422 | 787, 793 | backend/app/api/analytics.py | Existing `/liquidity` tests keep passing after simplifying to plain `await`; no new assertions beyond current 200-shape test |
| tests send undeclared query params (false confidence) | improvement | confirmed | `realized-risk?lookback_days=30` at test_coverage_deep_analytics_routes.py:84 and test_coverage_massive_api_sweep.py:74 (endpoint declares only `start`/`end`, lines 201-202); `performance-history?timeframe=…` at test_coverage_analytics_all_routes.py:115 (endpoint declares `days`, line 1179) — FastAPI ignores unknown params so assertions pass regardless | 201-202, 1179 (endpoints); cited test lines exact | backend/tests/test_coverage_deep_analytics_routes.py; backend/tests/test_coverage_massive_api_sweep.py; backend/tests/test_coverage_analytics_all_routes.py | Change one test to `realized-risk?start=…&end=…` and assert response `data_range` matches; change one to `performance-history?days=30` and assert series length ≤ ~30 |
| zero /tails tests; factor-exposure error only weakly asserted | improvement | confirmed | No test hits `/tails` or `/tail-dependence` (sweep calls nonexistent `/tail-risk` at test_coverage_massive_api_sweep.py:86, allowed by `assert status in [200, 404]` at :93); `test_analytics_engine.py:439` asserts only `"error" in empty_factor`; `test_wave2_transition.py:111` locks in `market == 1.0` | 2150-2151 (route); test lines exact | backend/tests/ (new test file or extend test_coverage_analytics_all_routes.py); backend/tests/test_analytics_engine.py; backend/tests/test_wave2_transition.py | GET `/analytics/tails?tickers=A.NS,B.NS` twice, assert 200 + `tail_dependence_matrix` shape on first call and cache hit on second; update engine test to assert `empty_factor["portfolio"]["alpha"] is None` once fixed |

## Optimizations

| id/short-name | severity* | verdict | evidence (1 line) | current line | files to touch | regression-test idea |
|---|---|---|---|---|---|---|
| /risk-score sequential fetch loop | optimization | confirmed | Lines 1021-1025 `for ticker in weights.keys(): df = await data_service.fetch_historical_data(...)` while `_fetch_price_series_dict` with Semaphore(5) exists at 136-163 | 1021-1025 | backend/app/api/analytics.py | Behavior-preserving swap: existing `/risk-score` tests pass unchanged; optionally spy that `fetch_historical_data` calls overlap (mock with async delay) |
| /liquidity-limits sequential fetch loop | optimization | confirmed | Lines 2091-2094 `for p in positions: df = await data_service.fetch_historical_data(...)`; sibling quote leg already `asyncio.gather`s at 809 | 2091-2094 | backend/app/api/analytics.py | Same as above: existing liquidity-limits tests must pass; gather-based fetch returns identical `price_dfs` dict |
| tear-sheet double wide-returns build | optimization | confirmed | Build at 1369-1372 (masked) and again at 1423-1425 (full-history); comment at 1421-1422 claims "second build is cache-served" but cold cache doubles vendor/DB work | 1369, 1423 | backend/app/api/analytics.py | Test with a counting mock on `_fetch_price_series_dict`: assert called once per tear-sheet request after fetch-once-and-slice refactor |
| /liquidity hand-rolled fetcher | optimization | confirmed | Lines 782-809 define local `fetch_liq` with its own Semaphore(5) duplicating `_fetch_price_series_dict` (136-163) | 782-809 | backend/app/api/analytics.py | Existing `/liquidity` tests pass after reuse; optional: assert identical results vs old path on a fixture frame |
| sync CPU work in async handlers | optimization | confirmed | `optimize(...)` at 1669 and `run_walk_forward_backtest` at 1735 are sync (`inspect.iscoroutinefunction` → False), copula fit at 2182-2185 likewise — they block the event loop; line 1669 sits inside `async def run_optimization` | 1669, 1735, 2182-2185 | backend/app/api/analytics.py | Existing optimize/backtest/tails tests must pass after wrapping in `asyncio.to_thread`; optional latency test: concurrent health ping stays responsive during a backtest |
| sequential cache-depth probe in tear-sheet | optimization | confirmed | Lines 1382-1386 `for t in ticker_list: cov = await get_cov(t)` — sequential awaits where `asyncio.gather` applies (DB reads, small gain as reported) | 1382-1386 | backend/app/api/analytics.py | Behavior-identical swap: existing tear-sheet tests pass; `full_start` equals `min(cached_starts)` as before |

\* severity column for Improvements/Optimizations reflects the report's section (no P-grade assigned there).

## Notes on report inaccuracies (did not change any verdict)

- Sentinel finding cites "same claim at 877" — line 877 is an error payload in stress-test; the actual duplicate descriptions are at **1877** (plus 1937, 2020, 2058, 2109, 2153). Main citation (line 200) is exact; finding stands.
- Improvement I11 says `test_analytics_engine.py:439` "locks in" the `{alpha:0, market:1}` shape — :439 only asserts `"error"` presence. The shape is actually locked in by `tests/test_wave2_transition.py:111` (`portfolio["market"] == 1.0`). Finding stands, stronger evidence cited above.
- Everything else matched the cited lines exactly; no line drift found in analytics.py.
