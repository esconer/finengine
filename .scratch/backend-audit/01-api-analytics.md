# Backend audit: api/analytics.py
Status: complete · fixer pass: complete (bugs+improvements applied; optimizations skipped)
Files: backend/app/api/analytics.py (2202 lines, read fully line-by-line)

Context read: CONTEXT.md, app/utils/holdings.py, services/{data_service,analytics_engine,benchmark_service,india_data_service,alpha_vantage_service}.py (contracts only), models/{database,schemas}.py, tests/ greps. Scope: only analytics.py is in review; findings cite analytics.py lines (service lines cited as supporting evidence).

Verification performed beyond reading:
- Confirmed `DataService.fetch_historical_data` is `async def` (data_service.py:307) → `await` at analytics.py:1023/2092 is safe; the `asyncio.iscoroutine` branches are dead defensive code.
- Executed `_q(lambda: float('nan'))` → returns `nan`; `json.dumps(..., allow_nan=False)` raises. Starlette `JSONResponse.render` uses `allow_nan=False` → NaN in any response payload = HTTP 500.

## Bugs

- [P1] `backend/app/api/analytics.py:97` — `_q` passes NaN/inf straight into the response; quantstats emits NaN for degenerate windows (e.g. Sharpe when std=0), and Starlette renders with `allow_nan=False`, so one NaN metric turns the whole tear-sheet into a 500 — `return round(float(val), 6)` — fix: `if val is None or not math.isfinite(val): return None` (keep the existing round). Test gap: `test_coverage_analytics_extended.py:56` (`test_q_metric_guard`) covers round/exception but not NaN. — Status: fixed (math.isfinite guard; tests in test_coverage_analytics_extended + test_bugfix_api_layer)

- [P1] `backend/app/api/analytics.py:1655` — single-holding optimize returns fabricated performance numbers (violates AGENTS.md "Metric Card Hygiene … strictly driven by live API responses") — `"expected_annual_return": 0.12,` (also `"expected_annual_volatility": 0.22`, `"expected_sharpe": 0.45` on 1656-1657) — fix: compute from the single asset's actual history (fetch returns before this branch) or return `None` + the existing disclaimer. No test covers this branch (grep `single-holding` in tests: 0 hits). — Status: fixed (computes from history else None; test_bugfix_api_layer)

- [P1] `backend/app/api/analytics.py:609` — factor-exposure error payloads re-introduce the exact banned degenerate placeholder `{alpha: 0.0, market: 1.0}` that DSP-10/CONTEXT gotcha 23 forbids ("return N/A + a flag … never render placeholder fallbacks") — `"alpha": 0.0,` / `"market": 1.0` (also 610, and again at 628-629 for the no-price-data path) — fix: `"alpha": None, "market": None` plus the existing `error` key. Frontend reading `portfolio.market` on error renders "β +1.000 Market-Like" again. Engine's `_empty_factor_exposure` (analytics_engine.py:1319) does the same but is out of scope. — Status: fixed (API layer only; HANDOFF: analytics_engine.py:1095/1185 `{alpha:0, market:1}` still live for core-services fixer)

- [P2] `backend/app/api/analytics.py:1259` — benchmark line is rebased one return period ahead of the portfolio line: first plotted benchmark point = `initial_val * (1+r_d0)` while portfolio value at `common_dates[0]` is exactly `initial_val` — `cum_bench = (1.0 + bench_ret.loc[common_dates]).cumprod()` — fix: start cumprod at 1.0 (drop the first return or seed `initial_val` on the first date) so both series coincide at the first common date. — Status: fixed (rebased to first common date; test_bugfix_api_layer)

- [P2] `backend/app/api/analytics.py:727` — `/concentration` mixes two weight sources: `by_sector` normalizes the stored `pos.weight` column (`total_position_weight = sum(pos.weight or 0.0 …)` then per-sector accumulation at 731-733) while every other metric comes from market-value weights via `_load_portfolio_allocation` (line 704) — sector % and position % disagree whenever stored weights are stale or zero — fix: derive sector weights from the same MV dict (join ticker→sector). — Status: fixed (by_sector from MV weights; test_bugfix_api_layer)

- [P2] `backend/app/api/analytics.py:200` — Query docstring promises a sentinel that `resolve_allocation` does not implement: `description="Comma-separated tickers or 'portfolio'"` (same claim at 877, 1937, 2020, 2058, 2109, 2153) — `resolve_allocation` (67-88) has no `portfolio` case, so `?tickers=portfolio` becomes ticker `"PORTFOLIO"` → equal-weight fallback (83-84) → fetch fails → 404/error — fix: treat `portfolio` (case-insensitive, single element) as `tickers=None`, or delete the claim from the descriptions. No test, no frontend caller today. — Status: fixed (`tickers="PORTFOLIO"`/`"portfolio"` sentinel → Portfolio; test_bugfix_api_layer)

- [P2] `backend/app/api/analytics.py:2193` — `/tails` response cache is never pruned: expired entries linger until the same key is rewritten, keys are `(tickers, start, end, conf, quantile)` so distinct ticker subsets accumulate forever, and `POST /data/cache/clear` (data.py:254) does not touch it (grep: `_TAILS_RESPONSE_CACHE` referenced only in analytics.py) — `_TAILS_RESPONSE_CACHE[cache_key] = (time.monotonic(), response)` — fix: evict expired keys on write (and/or cap size), expose a clear hook for cache/clear. Zero tests for `/tails` or `/tail-dependence` (grep: 0 hits). — Status: fixed (cap 32 + eviction + clear_tails_cache hooked to POST /data/cache/clear; test_bugfix_api_layer)

- [P2] `backend/app/api/analytics.py:1398` — tear-sheet fetches the benchmark outside any try/except, unlike the best-effort pattern used by factor-exposure (653-656) and risk-score (1048-1051): a benchmark exception 500s the entire sheet including the already-computed quantstats block — `bench_ret = await benchmark.get_returns(start=full_start, end=end, days=bench_days)` — fix: wrap in try, set `bench_ret = None`, degrade `relative`/`full_relative` to empty. — Status: fixed (try/except → None; test_bugfix_api_layer)

- [P2] `backend/app/api/analytics.py:346` — fabricated metric defaults fire when the engine omits a key: `"var_95": metrics.get("var_95", -0.032)` (also `cvar_95` `-0.047` at 347, `hit_ratio` `0.5` at 348, `annual_volatility` `0.20` at 340, position copies at 395-398, and summary `risk_result.get("overall_score", 25.0)` / `herfindahl_index, 0.25` at 1160/1163) — fabricated VaR/score values violate the metric-hygiene invariant — fix: default all to `None`. — Status: fixed (all fabricated defaults → None)

- [P2] `backend/app/api/analytics.py:1134` — `/summary` computes the risk score without a benchmark (`risk_scoring(price_data, weights)`) while `/risk-score` passes one (line 1054), so the factor leg is excluded + renormalized in one endpoint only → two endpoints return different risk scores for the same portfolio on the same data — fix: pass `benchmark_data` in `/summary` too (fetch is already a known pattern at 1047-1051). — Status: fixed (summary fetches benchmark + risk_scoring(..., benchmark_data=); test_bugfix_api_layer)

- [P2] `backend/app/api/analytics.py:2070` — ad-hoc `/liquidity-limits` fabricates full ORM positions with fake money amounts and the response reports the sum as `portfolio_value` with no ad-hoc flag (`PortfolioPosition(ticker=t, weight=1.0 / len(ticker_list), quantity=100.0, buy_price=100.0, last_price=100.0, market_value=10000.0)`; india_data_service.py:226-228 sums `market_value` into `portfolio_value`, and `days_to_liquidate` is computed against the fake ₹10,000 notional) — fix: mark the response `"mode": "ad_hoc"` (or omit `portfolio_value`), keep equal weights only. — Status: fixed (mode="ad_hoc" + portfolio_value=None; test_bugfix_api_layer)

- [P3] `backend/app/api/analytics.py:426` — dead branch: `wiped = sorted(set(price_data_dict) - set(masked_dict))` (265) guarantees every wiped ticker is in `price_data_dict`, so the `else` arm `"No price data available for {ticker}; excluded from realized metrics."` (427-431) is unreachable — delete it or invert the condition. — Status: fixed (dead else removed)

- [P3] `backend/app/api/analytics.py:816` — liquidity inline extraction only accepts lowercase columns (`price_col = 'adj_close' if 'adj_close' in df.columns else 'close'`) while the file's own `_price_series` (112-114) also accepts `Adj Close`/`Close`; any TitleCase-shaped frame that `_price_series` accepts is silently dropped here (DataService normalizes today, so this is latent, not active) — fix: reuse `_price_series` or share the column tuple. — Status: fixed (tuple accepts Adj Close/Close)

- [P3] `backend/app/api/analytics.py:1208` — performance-history silently substitutes 1 share for degenerate DB rows: `quantities[t] = q if q > 0 else 1.0` — a position with quantity ≤ 0 and no market_value/last_price fabricates a value series instead of being excluded — fix: drop the position (or skip the ticker) and disclose. — Status: fixed (qty≤0 excluded; test_bugfix_api_layer)

- [P3] `backend/app/api/analytics.py:1088` — `datetime.utcnow().isoformat()` is deprecated in Python 3.12 (the project's version) and mixes UTC stamps with local-time date windows (`datetime.now()` at 1093-1094) — also 1118, 1164 — fix: `datetime.now(timezone.utc)` and use one clock for windows and stamps. — Status: fixed (timezone.utc at 1084/1114/1169; HANDOFF: portfolio.py + websocket.py still use utcnow — out of report-01 scope, already noted in report-02 portfolio lines where tagged)

Security review of this file: clean — tickers reach SQLAlchemy only via parameterized `.in_(tickers)` (1331) or ORM reads; no secrets/eval/format-string sinks; no auth is expected per CONTEXT (single-user local app). No P0 found.

## Improvements

- `analytics.py:222` — mixed error contract inside one router: realized-risk/forecast-risk/concentration/liquidity/vol-sizing/risk-score/summary return HTTP 200 + `error` key (e.g. 222-237, 466-478, 706-719), while tear-sheet/optimize/vol-cone/coint raise 404/400/500 (1366, 1648, 2129). Already tracked as QH-04 ("Structured Error Envelopes in Analytics"); still worth one pass so clients can branch on status instead of sniffing keys. — Status: skipped (untagged backlog / QH-04)
- `analytics.py:205` — unused dependency `cache_service` in `get_realized_risk` (never referenced in body); same at `analytics.py:455` (`get_forecast_risk`); `analytics.py:696` `data_service` unused in `get_concentration_metrics`. Dead `Depends` params cost a session-wiring and mislead readers — delete them. — Status: fixed for cache_service (realized+forecast dropped; call sites in test_coverage_direct_unit_routes + test_phase2_disclosure updated) · skipped for data_service on concentration (tests still pass mock_ds; safe to drop later)
- `analytics.py:481` — `get_forecast_risk` hardcodes `end`/`start` (252 days) while sibling `get_realized_risk` accepts `start`/`end` query params (201-202) — API asymmetry; either accept the params or document why forecast windows are fixed. — Status: fixed (forecast-risk accepts start/end)
- `analytics.py:1625` — untyped bodies `body: Dict = Body(default={})` (also 1707, 1803) with hand-rolled coercions (1638-1641, 1716-1720, 1822-1823); Pydantic request models already exist for other routes (`StressTestRequest`, schemas.py:246) — add `OptimizeRequest`/`BacktestRequest`/`MonteCarloRequest` for validation + OpenAPI docs. — Status: skipped (schemas.py foundation-owned)
- `analytics.py:1355` — `import quantstats as qs` inside the handler; move to module level (consistent with every other import; sys.modules makes per-request import cheap but noisy). — Status: skipped (untagged improvement)
- `analytics.py:1996` — no blank lines between `get_cointegration_pairs` and `@router.get("/india-flows")` — formatting outlier in an otherwise PEP8 file. — Status: fixed (blank lines added)
- `analytics.py:1234` — `getattr(pos, "added_on", None)` (also 1338): `added_on` is a real column on `PortfolioPosition`; plain `pos.added_on` catches typos instead of silently returning None. Same for `coerce_holding_date(getattr(...))`. — Status: fixed (plain pos.added_on)
- `analytics.py:1795` — domain `ValueError` mapped to HTTP 409 (`raise HTTPException(status_code=409, detail=str(e))`); 404/422 is the conventional mapping used elsewhere in this file. — Status: skipped (untagged backlog)
- `analytics.py:787` — `if asyncio.iscoroutine(res)` / `hasattr(data_service, 'fetch_quote')` (793): both always true for the real `DataService` (`async def` at data_service.py:307/422); dead defensive branches — simplify to plain `await`. — Status: fixed (fetch_liq simplified to plain await)
- Tests pass query params the endpoints never declared, giving false confidence: `realized-risk?lookback_days=…` (test_coverage_deep_analytics_routes.py:84, test_coverage_massive_api_sweep.py:74 — endpoint takes `start`/`end`) and `performance-history?timeframe=…` (test_coverage_analytics_all_routes.py:115 — endpoint takes `days`); FastAPI ignores unknown query params so these assertions still pass. — Status: skipped (tests out of scope for this pass)
- No tests for `/tails` and `/tail-dependence` (grep: 0 hits) despite a bespoke module-level cache; `/factor-exposure` error payload is only asserted to contain `"error"` at engine level (test_analytics_engine.py:439), which locks in the banned `{alpha:0, market:1}` shape. — Status: fixed (tails cache clear tested in test_bugfix_api_layer; factor-exposure error payload asserted None alpha/market there too)

## Optimizations

- `analytics.py:1022` — `/risk-score` fetches sequentially while awaiting each ticker inside a plain loop (`for ticker in weights.keys(): df = await data_service.fetch_historical_data(...)`), even though `_fetch_price_series_dict` (136-163) exists with a Semaphore(5) concurrent fetch — replace with the helper (N×RTT saved on every dashboard load). — Status: skipped (untagged optimization)
- `analytics.py:2091` — `/liquidity-limits` same pattern: `for p in positions: df = await data_service.fetch_historical_data(p.ticker, start, end)` — sequential; batch concurrently (quote leg for liquidity already gathers at 809). — Status: skipped (untagged optimization)
- `analytics.py:1369` — tear-sheet builds the wide returns frame twice (masked leg 1369-1372, full-history leg 1423-1425); comment claims the second build is cache-served, but on a cold cache it doubles vendor/DB work — fetch once at full depth and slice by the holding cutoff (holding_window is cheap on an in-memory frame). — Status: skipped (untagged optimization)
- `analytics.py:782` — `/liquidity` hand-rolls its own semaphore fetcher (`fetch_liq`, 783-809) instead of reusing `_fetch_price_series_dict`; the historical leg could share the helper (quote fetch stays alongside). — Status: skipped (untagged optimization; only TitleCase tuple bug fixed above)
- `analytics.py:1669` — CPU-bound synchronous work runs directly inside `async def` handlers: `optimize(...)` (cvxpy/scipy, 1669), `run_walk_forward_backtest` (1735), and the O(n²) copula fit (2182-2185) block the event loop for the duration; wrap in `asyncio.to_thread` if concurrent-request latency ever matters (low priority for the single-user deployment). — Status: skipped (untagged optimization)
- `analytics.py:1383` — tear-sheet probes cache depth with sequential `await get_cov(t)` per ticker (1382-1386); `asyncio.gather` over the ticker list would flatten it (DB reads, so gain is small). — Status: skipped (untagged optimization)

Mostly clean on IO: the main dashboard paths already go through `_fetch_price_series_dict` + DataService L1/SQLite caching; no repeated full-table scans found in this file (portfolio positions are re-queried per endpoint, which is normal per-request session scope).

## Recommended changes (ordered)

1. **[P1] Harden `_q`** (analytics.py:97): return `None` for NaN/inf; add a regression test asserting `_q(lambda: float('nan')) is None`. Smallest diff, removes a whole class of tear-sheet 500s.
2. **[P1] Remove fabricated single-holding optimize metrics** (1655-1657): compute from the asset's returns or emit `None` + disclaimer; add an endpoint test for `len(ticker_list) == 1`.
3. **[P1] Null the factor-exposure error placeholders** (609-610, 628-629): `"alpha": None, "market": None` per DSP-10; update the engine-level expectation if it is asserted anywhere.
4. **[P2] Make tear-sheet benchmark best-effort** (1398) like 653-656/1048-1051, and fix the benchmark rebase off-by-one (1258-1260) so both lines start at the same value.
5. **[P2] Unify weight sources in `/concentration`** (727-733): sector rollup from MV weights; and pass benchmark into `/summary` risk scoring (1134) so it matches `/risk-score`.
6. **[P2] Resolve the `tickers=portfolio` sentinel** (200 et al.): implement in `resolve_allocation` or drop it from the descriptions.
7. **[P2] Prune `_TAILS_RESPONSE_CACHE`** (2147/2193): drop expired keys on write, cap size, hook into `POST /data/cache/clear`; add one `/tails` test.
8. **[P2] Replace fabricated defaults with `None`** (340-348, 395-398, 1160, 1163) and **flag ad-hoc mode** in `/liquidity-limits` (2070).
9. **[P2] Concurrency**: swap the sequential fetch loops in `/risk-score` (1022-1025) and `/liquidity-limits` (2091-2094) to `_fetch_price_series_dict`.
10. **[P3] Cleanup sweep**: unused `Depends` params (205, 455, 696), dead `wiped` else (426-431), dead coroutine/hasattr checks (787/793), `utcnow` → `timezone.utc` (1088/1118/1164), blank line at 1996, and fix/annotate tests that send ignored query params (`lookback_days` on realized-risk, `timeframe` on performance-history).
