# Verified: 04-quant-services.md

Verification pass (read-only; no code edited). Source re-read: all 10 cited service files, `analytics.py`, `data.py`, `schemas.py`, and cited tests. Empirical probes run from `backend/` via `uv run python -c ...`.

**Totals: 52 confirmed | 0 refuted | 0 already-fixed**

(Bugs 35/35 confirmed — P1 7, P2 10, P3 18 · Improvements 10/10 confirmed · Optimizations 7/7 confirmed)

## Bugs

| id/short-name | severity | verdict | evidence (1 line) | current line | files to touch | regression-test idea |
|---|---|---|---|---|---|---|
| coint cache key ignores params | P1 | confirmed | `cache_key = f"coint_{ticker_a}_{ticker_b}_{last_date}"` omits threshold/spread; `_db_cache_keys` sha1 likewise (`f"{ticker_a}|{ticker_b}"`); probe: dbkey `('COINT', 'coint_fa97dd47_2026-09-22')` — route passes both at analytics.py:1939-1941 | 255, 36-39 | backend/app/services/cointegration_service.py; backend/tests/test_p03_coint.py | Two `scan_pairs` calls with `p_value_threshold` 0.05 then 0.10 and `include_spread_series` False then True must return distinct results, not the first call's cache |
| max_half_life filters count only | P1 | confirmed | count predicate at `:391` gates `cointegrated_pairs_count` but `pairs=sorted_pairs` at `:399` is unfiltered → count ≠ `sum(p.is_cointegrated for p in pairs)`; `ou_half_life_days is None` pairs counted out yet listed | 389-399 | backend/app/services/cointegration_service.py | `scan_pairs(max_half_life=X)`: assert every listed cointegrated pair has `ou_half_life_days is not None and <= X`, or add explicit `half_life_filter_passed` flag |
| backtest unknown strategy silent EW | P1 | confirmed | probe: strategy `"bogus"` echoed in response, turnover 0.0, warnings at `backtest_service.py:63,76,158`; `ValueError` from `optimize` swallowed into equal-weight retention; route does no validation (analytics.py:1716) | 74-76 | backend/app/services/backtest_service.py; backend/app/api/analytics.py | POST backtest with `strategy: "bogus"` → 400; valid strategies still succeed |
| EVT fabricates GPD when n_u<5 | P1 | confirmed | `xi = 0.15`, `var = hist*1.15`, `es = hist*1.20` returned under real GPD keys with no flag; probe: n=60 → `n_u 3, xi 0.15, fat True`; `is_fat_tailed` always True (`:117`); `EVTPOTVarMetrics.gpd_shape_xi/gpd_scale_beta` non-optional (schemas.py:405) | 76-81 | backend/app/services/tail_risk_service.py; backend/app/models/schemas.py | 40-day history: response must expose `model_fitted=False` / `xi=None`, not 0.15; or raise like n<20 |
| indicator warmup too short | P1 | confirmed | `int(lookback_days*1.6)+120` calendar days; probe: lb=90 → 264 cal ≈ 175 trading rows < 200 → `close_200_sma` null for lookback ≲ 112-118; in `DEFAULT_INDICATORS` (`:50`); route default 90 (data.py:57); no test references `close_200_sma` | 146 | backend/app/services/indicators_service.py | GET `/indicators/{t}?lookback_days=90` asserts `close_200_sma is not None` |
| verified_snapshot hardcoded lookback=5 | P1 | confirmed | `compute_window(..., lookback_days=5, ...)` at `:209` → ~3-5 rows; `records[-look_back_days:]` can never return up-to-30 recent closes; `look_back_days` dead (data.py:83 default 30); test `test_data_services.py:109` asserts only `<= 10` | 209 | backend/app/services/indicators_service.py | Seed 30 days of history; GET `/indicators/{t}/snapshot?look_back_days=30` → `len(recent_closes) == 30` |
| MC no memory product cap | P1 | confirmed | `standard_normal((num_paths, steps))` at `:69`; probe: 20000×10080 = 201.6M elems = 1.61 GB/array, ×~4 live arrays ≈ 5 GB; route allows 40y/20000 (analytics.py:1863); same pattern `:95`, `:121` | 69 | backend/app/services/monte_carlo_service.py; backend/app/api/analytics.py | 40y/20000-paths call either succeeds under a `num_paths*steps` budget or returns 400 — never MemoryError |
| backtest final day excluded | P2 | confirmed | `rebalance_indices.append(len(returns) - 1)` + exclusive `iloc[t_start:t_end]` drops last day; probe: last_simulated 2024-10-03 vs frame 2024-10-04; `test_quant_math_p1_batch.py:139` pins `iloc[60:199]` for 200 rows | 45, 93 | backend/app/services/backtest_service.py; backend/tests/test_quant_math_p1_batch.py | 200-row frame: last simulated window must end at index 199; update pinning assertion |
| backtest no window validation | P2 | confirmed | probe: freq=0 → ValueError (opaque 400); negative lookback → no raise, garbage slices; `lookback_days=5` passes `len < lookback+freq` guard at `:35` and optimizes on 5 rows; route forwards raw ints (analytics.py:1717-1718) | 43 | backend/app/services/backtest_service.py; backend/app/api/analytics.py | `rebalance_freq_days=0` and `lookback_days=-1` → explicit 400 "invalid window", not step-0 crash |
| threshold ≥ confidence combo | P2 | confirmed | service never enforces `threshold_quantile < confidence_level`; route allows 0.90/0.98 (analytics.py:2155-2156); probe: `var90 -0.040433 ≥ u 0.040433` — GPD fit above reported confidence, clamp at `:105` forces 90% VaR up to 98% loss quantile | 72, 105 | backend/app/services/tail_risk_service.py | `confidence=0.90, threshold=0.98` → 400; normal 0.95/0.99 unchanged |
| ES clamp `var*1.05` | P2 | confirmed | `es_evt_loss = max(es_evt_loss, var_evt_loss * 1.05)` at `:106` forces ≥5% VaR→ES gap beyond real `ES ≥ VaR`; probe: analytic ES/VaR ratio 1.104 — clamp only inflates forced-path cases but violates honest bound | 106 | backend/app/services/tail_risk_service.py | Thin-tail fixture with ES ≈ VaR must not be bumped to `var*1.05` |
| BL excess/absolute view mix | P2 | confirmed | `pi = delta * (cov_ann @ w_mkt)` is excess prior (`:209`); views appended raw `q_vals.append(float(ret))` (`:220`); tangency does `excess = mu_bl - rf` (`:250`) — absolute view overweighted by ~rf in `Q - P@pi` (`:244`); docstring reads absolute; test only checks sum/sign (test_coverage_engines.py:85-93) | 209, 220, 244, 250 | backend/app/services/optimization_service.py | Absolute view `{T: 0.15}` with rf=0.06: assert weights match excess-convention reference (view − rf on ingest) |
| vol-cone `windows=[]` crash | P2 | confirmed | `windows[0]` at `:292` and eagerly-evaluated `next` default `window_results[0]` at `:294` both IndexError when `windows=[]`; `:219` only checks `is None`; probe confirmed IndexError; latent (route always defaults windows) | 292, 294 | backend/app/services/volatility_service.py | `calculate_volatility_cone(windows=[])` returns default-cone result, no raise |
| coint mem cache never evicted | P2 | confirmed | probe: no pop/del/LRU on `_IN_MEMORY_COINT_CACHE`; TTL checked only on hit (`:258-264`); every new as_of×pair inserts a key that lives forever; contrast bounded registry benchmark_service.py:105-107 | 24 | backend/app/services/cointegration_service.py | Insert keys past TTL+size: assert expired/over-cap entries purged on write |
| CPU work blocks event loop | P2 | confirmed | zero `to_thread` in analytics.py; heavy calls at simulate_goal@1857, run_walk_forward_backtest@1735, tails@2182/2185, vol-cone@2132, scan_pairs inner in async def; own comment ~12s for 14 assets (analytics.py:2143); correct pattern regime_service.py:310 | 159, 18, 228, 368, 151 | backend/app/api/analytics.py; backend/app/services/{monte_carlo,backtest,tail_risk,cointegration,volatility}_service.py | Concurrent health ping stays responsive during a backtest/tails call after `asyncio.to_thread` wrap |
| correlation `>` vs `>=` | P2 | confirmed | `is_regime_break = current > p90` (`:98`) but `alert_level` CRITICAL on `>=` (`:100`); probe: flat series → is_regime_break False, CRITICAL True; message claims "exceeds" | 98, 100 | backend/app/services/correlation_service.py | Flat series: `is_regime_break` and alert level must agree |
| benchmark get_returns ignores start | P2 | confirmed | `df = await self.ensure_history(days=days)` always fetches from `days` (default 756) then filters; `start` older than 756 without `days` → silent truncation; all 4 current callers safe (le=756 / pass days) — footgun not live defect | 82 | backend/app/services/benchmark_service.py | Call with `start` 1000 days ago + default days; assert returned frame covers full window (or fetch derives days from start) |
| regime n_components hardcoded 3 | P3 | confirmed | `startprob_=[0.33,0.34,0.33]` sticky 3×3 at `:197`; setters accept wrong shape, hmmlearn fit rejects ≠3; `_label_states_by_risk` non-3 branch unreachable; only caller n=3 (`:310`) | 106, 182-197 | backend/app/services/regime_service.py | `n_components=2` either works end-to-end or is rejected up front with clear ValueError |
| Parkinson annualized-then-averaged | P3 | confirmed | per-obs annualization then `rolling(10).mean()` of σ (`:149-150`); probe: mean-of-ann 0.0584 vs pooled 0.0711 — Jensen bias; spans 10 valid rows across gaps | 149-150 | backend/app/services/regime_service.py | Fixture with known logHL: pooled formula `sqrt(mean(logHL²)/(4ln2))*sqrt(252)` matches expected |
| regime docstring risk ordering | P3 | confirmed | module docstring `:4` says "ordered by risk (return/vol profile)" but `_label_states_by_risk` (`:47-50`) sorts cagr/ann_ret only; ann_vol never enters | 4, 47-50 | backend/app/services/regime_service.py | Docstring-only; any ranking test must state return-only ordering (or vol enters score) |
| EWMA empty returns 0.20 | P3 | confirmed | `return 0.20` at `:95` fabricated forecast; pinned by test_coverage_quant_services.py:86; route guards len<30 so unreachable in prod; inconsistent with no-fabrication policy | 95 | backend/app/services/volatility_service.py; backend/tests/test_coverage_quant_services.py | After fix: empty input → None+flag; update pinning assertion |
| vol-cone sorted() no-op | P3 | confirmed | `sorted([min_v, p25_v, ...])` is monotone by construction; probe: already sorted; dead code at `:268` | 268 | backend/app/services/volatility_service.py | Delete line; existing vol-cone tests unchanged |
| VolConeWindow.min rejects None | P3 | confirmed | service returns None for min/p25/… on short data (`:272-278`) but schema `VolConeWindow.min: float` non-optional (schemas.py:370); probe: ValidationError; route has no response_model so latent | 272-278 | backend/app/models/schemas.py | `VolConeWindow(min=None, ...)` validates once fields are Optional; short-history cone still 200 |
| tail beta fallback inconsistent | P3 | confirmed | `std(losses)*0.5` at `:79` vs `std(exceedances)` at `:111` — two fabrication formulas for beta | 79, 111 | backend/app/services/tail_risk_service.py | Moot after P1 removal; if kept, both paths must yield same beta on same fixture |
| copula short-circuit fabricated triple | P3 | confirmed | `<10` overlap → `(0.0, 0.0, 4.0)` with no flag (`:158`); largely unreachable via /tails (≥30 aligned) but fabricated in public method | 158 | backend/app/services/tail_risk_service.py | Pair with 5 overlapping rows: returns None/raises with flag, not fake df=4 |
| high_risk_pairs top-5 backfill | P3 | confirmed | when no λ≥0.20, `high_risk_pairs = pairs_list[:min(5, len)]` (`:261`) backfills with possibly all-LOW pairs; never honest "none" | 260-261 | backend/app/services/tail_risk_service.py | Low-dependence fixture: `high_risk_pairs == []` (or field renamed top_pairs) |
| copula docstring standardized marginals | P3 | confirmed | docstring claims "Student-t MLE fit on standardized marginals" (`:170`) but fit runs raw returns (`:172-173`); scale absorbs standardization; ν marginal used as copula df; ρ Pearson not rank-based | 170-174 | backend/app/services/tail_risk_service.py | Doc reword; existing λ_L value tests (test_p06_evt / invariants) unchanged |
| `_fan_from_paths` unused horizon_years | P3 | confirmed | body derives checkpoints from `paths.shape[1]` only (lines 140-148); `horizon_years` appears solely in signature line 137 annotated `int` while callers pass float (2.18) | 136-137 | backend/app/services/monte_carlo_service.py | Drop param; fan output identical (byte-for-byte) on fixture paths |
| `_as_matrices` twice per optimize | P3 | confirmed | called at `:292` and again `:305` for diagnostics (mu/cov of full frame recomputed); count=2 per non-HRP call | 292, 305 | backend/app/services/optimization_service.py | Spy/count `_as_matrices` → 1 call; optimize outputs unchanged |
| SolverError → 500 not 400 | P3 | confirmed | probe: `issubclass(cp.error.SolverError, ValueError) == False`; no catch in app; route maps only ValueError → 400 (analytics.py:1700-1702); backtest catches it; sites `:142/:159/:180` | 142, 159, 180 | backend/app/services/optimization_service.py | Monkeypatch solve to raise SolverError; POST /optimize/run → 400 |
| datetime.utcnow deprecated | P3 | confirmed | probe: DeprecationWarning on project Python (>=3.12); sites `:260/:278/:297/:307`; regime_service.py:341 already uses `now(timezone.utc)` | 260, 278, 297, 307 | backend/app/services/cointegration_service.py | `warnings.simplefilter("error")` around cache TTL check; no DeprecationWarning |
| mem cache key built twice + dead registry | P3 | confirmed | key hand-built at `:255` and `:293` (already diverged from `_db_cache_keys`); `benchmark_service._service_registry` only imported by test_coverage_services_and_api.py:20,206-207 — app DI at analytics.py:62 constructs BenchmarkService(db) directly; registry pins up to 64 strong refs | 255, 293 | backend/app/services/cointegration_service.py; backend/app/services/benchmark_service.py | Extract `_mem_cache_key`; delete registry (or wire it into route DI) — behavior-identical |
| dropna subset=[] no-op | P3 | confirmed | probe: shape unchanged (2,1) for `dropna(subset=[])`; only `"close"` recognized while `_close_series` (`:26-28`) knows four variants; DataService lowercases today | 68 | backend/app/services/benchmark_service.py | TitleCase frame through get_returns path: no crash, rows dropped via `_close_series` columns |
| `_ensure_date_column` KeyError reachability | P3 | confirmed | renames only `("date","index","Datetime")` (`:75-78`); unknown index name falls through → `_clean_dataframe` `data["Date"]` KeyError (probe: `KeyError 'Date'`); DataService-can-produce-unverified — footgun as reported | 75-78 | backend/app/services/indicators_service.py | Frame with index name `timestamp`: no KeyError; gets renamed/defaulted to Date |
| CorrelationService pure pass-through | P3 | confirmed | `:151-168` both methods delegate to module functions; used only by tests — duplication surface only | 151-168 | backend/app/services/correlation_service.py | Existing correlation tests pass whether wrapper kept or deleted |

## Improvements

| id/short-name | severity* | verdict | evidence (1 line) | current line | files to touch | regression-test idea |
|---|---|---|---|---|---|---|
| tail_risk mixed error strategies | improvement | confirmed | Root cause of P1: three paths (raise n<20, fabricate n_u<5, clamp) with no machine-readable indicator; forced by non-optional `EVTPOTVarMetrics` fields (schemas.py:405-406) | 76-81 | backend/app/services/tail_risk_service.py; backend/app/models/schemas.py | `model_fitted: bool` present on all EVT responses; n_u<5 fixture sets it False |
| backtest conflates input vs solver errors | improvement | confirmed | One `except Exception` (`:74`) swallows unknown-strategy ValueError (fail-fast) together with legitimate per-window solver failures (fallback correct) — root cause of P1 backtest finding | 74-76 | backend/app/services/backtest_service.py | Unknown strategy raises at entry; CLARABEL failure mid-backtest still falls back with warning |
| SolverError → ValueError at 3 sites | improvement | confirmed | Same as P3 SolverError finding — the improvement restates the fix for API 400 mapping | 142, 159, 180 | backend/app/services/optimization_service.py | See P3 SolverError test |
| typing/contracts drift | improvement | confirmed | VolConeWindow non-optional floats vs service Nones (P3); `evt_pot_var_99` keys hardcoded while confidence free 0.90–0.999 (`tail_risk_service.py:121-124`) | 121-124; schemas.py:370 | backend/app/services/tail_risk_service.py; backend/app/models/schemas.py | confidence=0.95 response: key names or schema reflect actual confidence level |
| bench_weights naming | improvement | confirmed | `backtest_service.py:51` — both mutable, `bench_weights` never changes (buy-and-hold) | 51 | backend/app/services/backtest_service.py | Rename-only; existing turnover/return tests unchanged |
| dead code list | improvement | confirmed | `_fan_from_paths.horizon_years`, vol-cone `sorted`, `_service_registry` unused by app, `_label_states_by_risk` non-3 branch unreachable — each verified above | see P3 rows | backend/app/services/{monte_carlo,volatility,benchmark,regime}_service.py | Grep/tests: removed symbols gone; suites green |
| duplication list | improvement | confirmed | `_as_matrices` ×2 (P3), mem cache-key ×2 (P3), inconsistent tail beta fallbacks (P3) — each verified | see P3 rows | backend/app/services/{optimization,cointegration,tail_risk}_service.py | See corresponding P3 tests |
| datetime.utcnow 4 sites | improvement | confirmed | Same as P3 utcnow finding (4 call sites in cointegration_service) | 260, 278, 297, 307 | backend/app/services/cointegration_service.py | See P3 utcnow test |
| doc/impl drift list | improvement | confirmed | Regime risk ordering, BL view convention, tail copula marginals, coint max_half_life "filter" — all four verified above | see P3/P2 rows | backend/app/services/{regime,optimization,tail_risk,cointegration}_service.py | Doc-only for three; max_half_life gets the count/list invariant test |
| tests to add (8 gaps) | improvement | confirmed | grep of backend/tests/ confirms absent: param-keyed coint cache, max_half_life invariant, unknown-strategy raise, final-day inclusion, close_200_sma non-null, verified_snapshot look_back_days, EVT honesty flag, threshold<confidence guard, paths×steps cap | — | backend/tests/ (new/extended) | Each listed gap becomes one focused test; all start red, flip green with the P1/P2 fixes |

## Optimizations

| id/short-name | severity* | verdict | evidence (1 line) | current line | files to touch | regression-test idea |
|---|---|---|---|---|---|---|
| stats.t.fit per pair in O(n²) loop | optimization (P2) | confirmed | `stats.t.fit(r_a)` recomputed every pair inside loop (`:172-173`, loop `:228`/`:232`); df_a depends only on ticker i; explains "~12s for 14 assets" comment (analytics.py:2143) | 172-174 | backend/app/services/tail_risk_service.py | Output matrix identical pre/post hoist; optional: fit-count spy = n not n(n−1) |
| serial DB awaits per coint pair | optimization (P2) | confirmed | probe: serial `await _get_cached_pair` (`:363`) + `_set_cached_pair` (`:378`) per pair → n(n−1)/2 round-trips; single-AsyncSession gate (CONTEXT gotcha 21) | 363, 378 | backend/app/services/cointegration_service.py | Behavior-identical scan; optional round-trip spy shows batched reads/writes |
| event-loop blocking (dup of P2) | optimization (P2) | confirmed | Same finding as Bugs/P2 — zero to_thread at heavy call sites; regime_service.py:310 is the pattern | 159, 18, 228, 368, 151 | backend/app/api/analytics.py; services | See Bugs/P2 event-loop test |
| MC memory cap/chunk (dup of P1) | optimization (P2) | confirmed | Same finding as Bugs/P1 — `num_paths * steps` unbounded; fix = `min(num_paths, budget // steps)` or chunk | 69, 95, 121 | backend/app/services/monte_carlo_service.py | See Bugs/P1 MC product-cap test |
| reuse mu/cov in optimize | optimization (P3) | confirmed | Second `_as_matrices` at `:305` duplicates `:292` — same as P3 finding | 305 | backend/app/services/optimization_service.py | See P3 `_as_matrices` count test |
| O(N²) rolling().corr() | optimization (P3) | confirmed | `correlation_service.py:54-58` pairwise rolling corrs; fine at personal-portfolio scale; YAGNI — do not preemptively rewrite | 54-58 | backend/app/services/correlation_service.py | No test until N grows; existing correlation tests must stay green if ever refactored |
| backtest iterrows | optimization (P3) | confirmed | `test_chunk.iterrows()` (`:94`) row-at-a-time; `test_chunk.to_numpy() @ current_weights` vectorizes; keeps day-0 cost branch | 94 | backend/app/services/backtest_service.py | Vectorized P&L equals iterrows reference on fixture (allclose); existing turnover tests green |

\* severity column for Improvements/Optimizations reflects the report's section (no P-grade assigned there except where the report tagged Optimize rows).

## Notes on report inaccuracies (did not change any verdict)

- Route line numbers for cache-key params cited as `analytics.py:1984-1986` in the report; current file has them at **1939-1941** (report likely based on slightly older line map — finding content exact, line drifted).
- P1 indicator finding cites `data.py:83` for look_back_days and `data.py:57` for default lookback — both exact.
- `_fan_from_paths` probe initially printed `horizon_years in body → True` because the signature line matched; body read confirms the parameter is never referenced below line 137 — finding stands as written.
- Everything else matched cited lines exactly; no other line drift in the 10 service files.
