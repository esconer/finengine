# Backend audit: foundation (models/db/migrations/utils)
Status: complete
Files:
- backend/app/models/schemas.py (568 lines)
- backend/app/models/database.py (215 lines)
- backend/app/config.py (58 lines)
- backend/app/db/database.py (101 lines)
- backend/app/utils/holdings.py (244 lines)
- backend/app/utils/logger.py (45 lines)
- backend/migrations/cleanup_duplicates_and_add_constraints.py (281 lines)
- backend/migrations/add_portfolio_columns.py (84 lines)
- backend/app/__init__.py, app/models/__init__.py, app/services/__init__.py, app/api/__init__.py (3 lines each)
- backend/_diag_rc.py (64 lines)
- backend/test_api_integrity.py (295 lines)
- backend/test_bulk_operations_integrity.py (338 lines)

Verification notes: pydantic 2.12.3 / pydantic-settings 2.11.0 (from uv.lock); runtime checks executed read-only via `uv run python` for `Field(env=)`, `min_items`, `ALLOWED_ORIGINS` env parsing, and SQLAlchemy `server_default` with explicit `None`; greps run against backend/tests/ for coverage.

## Bugs

- [P1] `backend/migrations/add_portfolio_columns.py:51` — migration fabricates quantity and cost basis for every pre-existing row, producing wrong unrealized P&L forever after (ALTER ADD COLUMN backfills legacy rows with DEFAULT 0.0, so `WHERE quantity = 0.0` matches ALL of them on first run); violates the AGENTS.md no-fabricated-fallbacks invariant in spirit — `SET quantity = 100.0, buy_price = market_value / 100.0` — fix: leave legacy rows NULL/0 and surface "unknown cost basis" in the API, or require user input; never invent quantity=100. Status: fixed (fabricating UPDATE deleted; backup + conn=None added; tests/test_bugfix_foundation.py)
- [P1] `backend/app/db/database.py:43` — `os.makedirs("data", exist_ok=True)` runs AFTER `create_all` at line 40, so it can never prevent the failure it exists for; verified: sqlite3/aiosqlite raise `OperationalError: unable to open database file` when `./data` is missing (confirmed with a repro). CWD-dependent URL (`sqlite+aiosqlite:///./data/daisy.db`, config.py:14) makes this reachable whenever the server is started outside `backend/` — `await conn.run_sync(Base.metadata.create_all)` … `os.makedirs("data", exist_ok=True)` — fix: move makedirs (or `os.makedirs` of the DB dir derived from `settings.database_url`) before `engine.begin()`. Status: fixed (ensure_sqlite_dir() via make_url runs before create_all in init_db/create_tables)
- [P1] `backend/test_bulk_operations_integrity.py:14` — imports a function that does not exist anywhere in the codebase (grep: only references are lines 14/26 of this file; `app/db/database.py` exports `init_db/create_tables/drop_tables/close_db_connections/get_db_session` only), so the script dies with ImportError before doing anything — `from app.db.database import create_database_engine` — fix: delete the script (it is superseded by tests/) or rewrite against `SessionLocal`. Status: fixed (script deleted; repo grep: no importers)
- [P1] `backend/test_bulk_operations_integrity.py:175` — rollback test asserts the opposite of its own message: `final_count >= initial_count` PASSES when records WERE wrongly added (rollback failure ⇒ count increases ⇒ assertion passes); it can only fail if rows were deleted — `assert final_count >= initial_count, "Transaction rollback failed - records were added despite validation error"` — fix: `assert final_count == initial_count`. Status: fixed (script deleted with B3)
- [P1] `backend/test_bulk_operations_integrity.py:271` — cleanup deletes the user's REAL holdings: the OR includes any position whose ticker is MSFT/GOOGL/TSLA/AAPL regardless of custom_name, i.e. live portfolio data loss when this script is run as intended — `DELETE FROM portfolio_positions WHERE custom_name LIKE '%Test%' OR ticker IN ('MSFT', 'GOOGL', 'TSLA', 'AAPL')` — fix: delete only by the exact test custom_names/ids created in this run, or delete the script. Status: fixed (script deleted; no importers)
- [P1] `backend/test_api_integrity.py:44` — writes into the live holdings-truth table (`POST /portfolio/bulk_add` with MSFT/GOOGL, plus NVDA at line 203) and never deletes anything anywhere in the file, permanently polluting the user's portfolio and weight normalization — `f"{self.base_url}/api/v1/portfolio/bulk_add",` — fix: delete created rows at teardown (by the `custom_name` sent) or remove the script. Status: fixed (script deleted; no importers)
- [P2] `backend/app/config.py:35` — `debug` defaults to `True`, and `backend/app/db/database.py:20` passes it straight to SQL echo, so every statement is logged to stdout by default in any environment that doesn't set DEBUG — `debug: bool = Field(default=True, env="DEBUG")` / `echo=settings.debug,` — fix: `debug: bool = Field(default=False, ...)` and/or `echo=False` unconditionally (or `echo= settings.debug and settings.environment == "development"`). Status: fixed (debug default False; engine echo = debug AND development)
- [P2] `backend/app/config.py:39` — `allowed_origins: list[str]` from env requires a JSON array; the natural comma-separated value crashes at import time (verified: `ALLOWED_ORIGINS="http://a:3000, http://b:3000"` → `SettingsError: error parsing value for field "allowed_origins" from source "EnvSettingsSource"`, raised by the module-level `settings = Settings()` on line 58, taking down the whole app) — `allowed_origins: list[str] = Field(` — fix: accept a comma-separated string and split in a validator, or document JSON-only and fail with a clear message. Status: fixed (NoDecode + before-validator accepts JSON array OR comma-separated)
- [P2] `backend/app/models/database.py:81` — mutable shared default: every `AnalyticsCache()` instance can receive the SAME dict object (`default={}` is not callable), a classic SQLAlchemy footgun — `model_params = Column(JSON, default={})` — fix: `default=dict`. Status: fixed
- [P2] `backend/app/models/database.py:136` — NSE tables expose only non-unique composite indexes (`ix_bhav_symbol_date`; same pattern at :155 `ix_flow_date_cat`, :177, :199), so uniqueness is enforced only by app-level select-then-insert in `india_data_service` (lines 69–78, 111–119). Two concurrent ingests of the same date create duplicates; a later `scalar_one_or_none()` then raises `MultipleResultsFound` — `Index("ix_bhav_symbol_date", "symbol", "date"),` — fix: `UniqueConstraint("symbol", "date")` / `("date","category")` and use `ON CONFLICT`. Status: fixed on the model side (bhavcopy, institutional flows, shareholding → UniqueConstraint; ON CONFLICT in india_data_service → HANDOFF below). PARTIAL REFUTE for :177 NSEBulkBlockDeal — (symbol, date) is NOT its natural key (NSE bulk/block pages list multiple client trades per symbol per day), left as plain Index with evidence comment
- [P2] `backend/migrations/add_portfolio_columns.py:74` — `conn` is assigned inside `try` (line 22); if `sqlite3.connect` itself raises, `finally` raises `UnboundLocalError`, masking the original exception — `if conn:` — fix: `conn = None` before `try`. Status: fixed
- [P2] `backend/migrations/add_portfolio_columns.py:49` — destructive data rewrite with NO backup step, unlike `cleanup_duplicates_and_add_constraints.py:217` which backs up first — `UPDATE portfolio_positions` — fix: call the same SQLite backup-API routine before mutating. Status: fixed (create_backup() runs before any ALTER)
- [P2] `backend/migrations/cleanup_duplicates_and_add_constraints.py:17` — CWD-relative DB path; `sqlite3.connect` CREATES an empty file when run from the wrong directory, then line 42 crashes with uncaught `no such table: stock_timeseries`, leaving a stray empty `data/daisy.db` behind — `def __init__(self, db_path: str = "data/daisy.db"):` — fix: resolve via `Path(__file__).parent.parent / "data" / "daisy.db"` like add_portfolio_columns.py:12, and refuse to run if the file doesn't already exist. Status: fixed (absolute default + FileNotFoundError in __init__; backup_dir derived from db_path)
- [P2] `backend/app/models/schemas.py:13` — `PortfolioPositionBase.ticker` has NO `pattern`, so the AGENTS.md NSE/BSE invariant (`^[A-Z0-9\-\&\.]{1,20}$`, hyphens/alphanumeric/exchange suffixes) is encoded only in `ValidateTickerRequest` (line 165) and API-layer `portfolio._TICKER_PATTERN` (portfolio.py:32) — and portfolio's single `POST /add` path checks vendor existence (portfolio.py:170) but not the format regex, while `POST /bulk_add` checks the regex (portfolio.py:309). Three layers, inconsistent contracts; any non-API consumer of the schema accepts `"BAD TICKER!"` — `ticker: str = Field(..., min_length=1, max_length=20, description="Stock ticker symbol")` — fix: add `pattern=r"^[A-Z0-9\-\&\.]{1,20}$"` to the field (validator already uppercases at line 25). Status: fixed (pattern added; uppercase validator moved to pre=True so lowercase input still legal)
- [P2] `backend/test_api_integrity.py:142` — business-rule test can never fail: both the 400 branch (line 135) and the else branch return True; also line 96 accepts HTTP 500 as a PASS for invalid tickers (masks server crashes), and line 275 declares overall success at a 60% pass threshold — `return True` (under `# May still work depending on implementation`) — fix: treat unexpected status as failure; require 100%; delete the script if superseded by pytest. Status: fixed (script deleted; test_bug_sweep bulk-malformed-ticker assertion extended for the new schema-level 422 path)
- [P3] `backend/app/config.py:14` — `Field(..., env="DATABASE_URL")` is a verified no-op on pydantic-settings 2.11 (runtime check: pydantic v2 raises `PydanticDeprecatedSince20 … Extra keys: 'env'` — the kwarg lands in `json_schema_extra`, not env binding). Env vars only work because every field name uppercased happens to equal the intended var. Renaming any field silently breaks its env override — `database_url: str = Field(default="sqlite+aiosqlite:///./data/daisy.db", env="DATABASE_URL")` — fix: drop all `env=` kwargs and rely on field-name matching (documented), or use `Annotated[..., Env(...)]` / `validation_alias`. Status: fixed (all env= kwargs dropped; name-uppercasing contract documented in config.py docstring + tested)
- [P3] `backend/app/utils/logger.py:22` — `LOG_LEVEL` is silently ignored: docstring/comment claims environment sourcing but the code only honors the parameter; `settings.log_level` (config.py:36) has zero consumers (grep verified; uvicorn's level is hardcoded at main.py:178) — `log_level = level or "INFO"` — fix: `log_level = level or settings.log_level`. Status: fixed (wired + typo-safe level parse so a bad LOG_LEVEL cannot crash setup)
- [P3] `backend/app/db/database.py:37` — comment promises work the code doesn't do; `init_db` creates whatever happens to be registered. Safe today only because main.py:22 imports all routers (which import models) before lifespan runs at main.py:59 — `# Import all models to ensure they're registered` — fix: add `from app.models import database as _models  # noqa: F401` or delete the comment. Status: fixed (explicit import inside init_db — function-level to avoid circular import)
- [P3] `backend/app/utils/holdings.py:141` — bare `except Exception` passes the series through UNMASKED on any error, silently re-attributing pre-purchase price action — the exact failure this module's docstring (lines 1–13) exists to prevent — `masked[ticker] = series` (in the `except` at line 140) — fix: at least `logger.warning` the ticker, or drop the ticker from the masked dict so the caller sees the gap. Status: fixed (warn WITH exc_info + drop — neither passthrough nor silence)
- [P3] `backend/app/utils/holdings.py:242` — `str(value)[:10]` returns any junk (`"abc"`, `"None"`) as if it were an ISO date; only safe because downstream `pd.Timestamp(...)` calls are wrapped in try/except — `return str(value)[:10] or None` — fix: validate with `date.fromisoformat` and return None on failure. Status: fixed

## Improvements

- `backend/app/models/schemas.py:27` — `weight_must_be_valid` (lines 27–31) plus quantity/buy_price validators (33–43) exactly duplicate the `Field(gt=…, le=…)` constraints on lines 14–16; same for `added_on` future-date validator duplicated at 45–49 and 65–69. Delete the redundant validators; keep one copy. Status: skipped (untagged backlog)
- `backend/app/models/schemas.py:7` — `validator` is pydantic-v1 deprecated (works, warns); `min_items` at line 151 is also deprecated (verified still ENFORCED in 2.12, so not a functional bug — use `min_length=1`); `datetime.utcnow` at line 289 is removed-in-3.14 deprecated → `datetime.now(timezone.utc)`. Status: partially fixed (min_items→min_length and utcnow→now(timezone.utc) swapped; validator→field_validator conversion skipped, untagged backlog)
- `backend/app/models/schemas.py:17` — `region` defaults to `"US"` in an India-first (.NS/.BO) app; every position created without an explicit region is tagged US — `region: str = Field(default="US", …)` — fix default to `"IN"` or derive from ticker suffix. Status: skipped (untagged backlog — behavior/product change)
- `backend/app/models/schemas.py:264` — `target_volatility: Optional[float] = 0.15` accepts negative/absurd values at the schema layer (no `gt=0, lt=1`); `:295` `positions: List[…]` accepts an empty bulk request (no `min_length=1`). Status: skipped (untagged backlog)
- `backend/app/db/database.py:50` — `get_db_session` double-manages the session: `async with SessionLocal()` already closes; the `except`+`finally: await session.close()` (lines 53–57) is redundant (rollback on error is fine but could be a `finally` guard) — simplify to try/except rollback only. Status: skipped (untagged backlog)
- `backend/migrations/cleanup_duplicates_and_add_constraints.py:138` — `ALTER TABLE … ADD CONSTRAINT … UNIQUE` is not supported by ANY SQLite version; this always raises `OperationalError` and is swallowed at line 143 with a misleading "may already exist or not supported" log — delete the block (the `CREATE UNIQUE INDEX IF NOT EXISTS` at 125 is the real mechanism). Status: skipped (untagged backlog)
- `backend/migrations/cleanup_duplicates_and_add_constraints.py:175` — validation probes the live table by INSERTing `'TEST'` rows; safe only by luck of transaction scoping — use a temp table or assert via `PRAGMA index_list` instead. Status: skipped (untagged backlog)
- `backend/migrations/cleanup_duplicates_and_add_constraints.py:19` — `backup_dir = "data/backups"` accumulates one full DB copy per run with no retention; additionally one such backup IS committed to git (verified `git ls-files`: `backend/data/backups/daisy_backup_20251102_183018.db`) — user portfolio data in the repo. Untrack it, gitignore `data/backups/`, prune old backups. Status: skipped (untagged backlog; git/.gitignore outside this fixer's scope)
- `backend/app/utils/logger.py:26` — `getattr(logging, log_level.upper())` raises AttributeError for a typo'd level; `logger.propagate` is never disabled, so any root-handler configuration double-logs. Use `logging.getLevelName`/validation and set `propagate = False` after adding the handler. Status: partially fixed (typo-safe level validation added — mandatory now that settings.log_level is wired, B17; propagate=False skipped — breaks caplog-based test capture and no root handlers exist in prod)
- `backend/_diag_rc.py` — root-level debug debris: `import numpy as np` sits at line 23 AFTER `frame()` is defined (works only because `frame` is called later), global `warnings.filterwarnings("ignore")` at line 2, and bare `asyncio.run(main())` at line 64 executes on import. Delete or move under `scripts/` with an `if __name__ == "__main__"` guard. Status: fixed (script deleted; no importers)
- `backend/app/models/database.py:33` — `stock_data` relationship + `position_id` FK (line 57) are dead: grep shows `position_id` is never assigned anywhere; with `PRAGMA foreign_keys=ON` now active, if it ever were populated, `db.delete(position)` (portfolio.py:720) has no `cascade`/`ondelete` and would FK-fail. Either wire `ondelete="SET NULL"` + document, or drop the FK/relationship. Status: skipped (untagged backlog)
- `backend/app/models/database.py:184` — `NSEShareholdingPattern` and `NSEBulkBlockDeal` (line 162) have no writer anywhere (grep: definitions + cache-purge map only); fine as t28/t29 scaffolding, but note they are schema-without-usage. Status: skipped (untagged backlog)
- Test coverage gaps (grepped backend/tests/): `config.py`, `logger.py`, both migrations, `db/init_db` ordering, and all three root scripts have ZERO tests; `holdings.py` is fully covered (tests/test_holding_aware.py exercises every public function incl. `portfolio_regime_summary`, `coerce_holding_date`, `implied_start_from_price`; tests/test_phase2_disclosure.py covers `holding_coverage`); ticker-pattern invariant covered at tests/test_contract_p1_batch.py:217–227; `added_on` future-date + round-trip covered at tests/test_api_endpoints.py:174–233. Status: fixed (tests/test_bugfix_foundation.py adds Settings env parsing, init_db missing-dir/model-registration, both migration paths, NSE uniqueness, schema bounds, LOG_LEVEL; trap-script deletion gated; holdings/ticker coverage extended)

## Optimizations

- `backend/app/db/database.py:20` — `echo=settings.debug` with debug defaulting True (config.py:35) is a throughput/log-volume tax on every query; decouple SQL echo from `debug` entirely (see Bugs P2 above) — `echo=settings.debug,`. Status: fixed (via B7: echo = debug AND environment == "development")
- `backend/app/models/database.py:80` — `expires_at` is filtered with no index (cache_service.py:129/161 purge paths filter on `expires_at` alone → full scan of `analytics_cache`). Add `Index("ix_expires_at", "expires_at")` or accept it while the table stays small — `expires_at = Column(DateTime(timezone=True), nullable=False)`. Status: skipped (untagged backlog)
- `backend/app/models/database.py:94` — `fetch_logs` grows unbounded (insert-only via `cache_service.log_fetch_attempt`); no retention/purge exists. Add a periodic delete of rows older than N days — `class FetchLog(Base):`. Status: skipped (untagged backlog)
- `backend/migrations/cleanup_duplicates_and_add_constraints.py:125` — creates a SECOND unique index `idx_stock_timeseries_unique_ticker_date` on `(ticker, date)` when the ORM already declares `UniqueConstraint("ticker", "date")` (models/database.py:62) → two unique indexes over the same columns, doubling index-maintenance cost on every upsert. Same drift on `analytics_cache`: ORM index is NON-unique `ix_ticker_metric` (models/database.py:85) while the migration adds UNIQUE `idx_analytics_cache_unique` (line 131) — fresh create_all DBs and migrated DBs end up with different schemas. Make the ORM the single source of truth and drop the redundant migration index. Status: skipped (untagged backlog — multi-file ORM/migration reconciliation)
- Engine lifecycle is clean: one module-level engine + `async_sessionmaker` (db/database.py:18–29), `expire_on_commit=False`, `close_db_connections()` for teardown — no repeated engine creation found. Status: n/a (negative finding stands)

## Cross-area schema prep delivered (other fixers depend on these)

- `EVTPOTVarMetrics` (schemas.py): `gpd_shape_xi`/`gpd_scale_beta` are now `Optional[float] = None` and `model_fitted: bool = True` exists — report 04 tail-risk P1 can return `xi=None, model_fitted=False` on the n_u<5 fallback. **HANDOFF → quant fixer:** set those in `tail_risk_service.calculate_evt_pot_var_es` fallback path.
- `VolConeWindow` (schemas.py): `min/p25/median/p75/max` are `Optional[float]` — service None quantiles are now schema-legal (report 04 P3 complete at schema layer; no service edit needed).
- `CustomScreenRequest` (schemas.py): `Field` bounds added mirroring the prebuilt route (`max_stocks ge=5 le=100 default 50`; ratios `ge=0` + `le=100` where percentages; `max_pe ge=0 le=1000`; `min_mcap_cr ge=0`) — report 02 P2.

## HANDOFF (cross-area — NOT edited by this fixer)

1. **`app/services/india_data_service.py` (B10 remainder):** `NSEBhavcopy` and `NSEInstitutionalFlow` now declare `UniqueConstraint`s; switch the select-then-insert writers (bhav ~:69-78, flow ~:111-119) to `ON CONFLICT DO NOTHING`/`DO UPDATE` or catch `IntegrityError`+reselect, so a concurrent same-date ingest completes instead of 500ing (dedupe intent: same (symbol,date) / (date,category) collapses to one row).
2. **`app/services/tail_risk_service.py` (report 04 P1):** schema now legalizes `gpd_shape_xi=None`/`gpd_scale_beta=None` + `model_fitted` — set `model_fitted=False` and `xi=None` (honest historical-only VaR/ES) on the n_u<5 fabricated path (`xi = 0.15` / `hist*1.15` / `hist*1.20` currently).
3. **`app/api/portfolio.py:309` (optional cleanup):** route-level `_TICKER_PATTERN` check is now redundant with the schema `pattern=` on `PortfolioPositionBase.ticker` (keep for defense-in-depth or drop — behavior-neutral for valid input; malformed input now 422s at schema before the route runs). `test_bug_sweep_2026_09.py` malformed-ticker assertion already accepts both shapes.

## Recommended changes (ordered)

1. Delete or quarantine the three root scripts (`test_bulk_operations_integrity.py`, `test_api_integrity.py`, `_diag_rc.py`): one is unrunnable (ImportError at line 14), one has an inverted assertion (line 175), and between them they delete/pollute live `portfolio_positions` (lines 271 / 44) while scoring false passes (line 142, 96).
2. Fix `add_portfolio_columns.py`: stop fabricating `quantity=100.0` / `buy_price=market_value/100.0` (line 51), add a backup step, and initialize `conn = None` before `try`.
3. Fix `db/database.py` startup order: create the data directory BEFORE `create_all` (line 43 → before line 36); make the path derivation CWD-independent.
4. Default `debug=False` in config.py:35 and remove `echo=settings.debug` coupling (db/database.py:20).
5. Make `ALLOWED_ORIGINS` accept comma-separated values or document/enforce JSON (config.py:39) — currently a verified import-time crash.
6. Add the ticker `pattern=` to `PortfolioPositionBase.ticker` (schemas.py:13) so the NSE/BSE invariant lives in one place instead of two-and-a-half.
7. Add `UniqueConstraint` to the NSE tables' natural keys and switch `india_data_service` dedupe to `ON CONFLICT` (models/database.py:136/155).
8. Reconcile ORM vs migration indexes (drop `idx_stock_timeseries_unique_ticker_date`, decide unique-vs-nonunique for `ix_ticker_metric`) and untrack/prune `data/backups/*.db`.
9. Small cleanups: `default=dict` on `model_params`, dead-comment removal in `init_db`, `settings.log_level` wiring in logger, drop redundant pydantic validators, uncommitted-deprecation swaps (`validator`→`field_validator`, `min_items`→`min_length`, `utcnow`→`now(timezone.utc)`).
10. Add minimal tests for config parsing (esp. ALLOWED_ORIGINS), `init_db` on a missing directory, and one migration happy-path — these files are currently 0% covered.
