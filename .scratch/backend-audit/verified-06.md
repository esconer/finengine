# Verified audit: 06-foundation

Verifier: bug-verifier (read-only). Method: re-read every cited line + read-only probes (`uv run python`, sqlite URI `mode=ro`, greps, `git ls-files`). config.py re-checked against current file — cited lines 14/35/39/58 all still match exactly (no ±3 drift on cited lines). Nothing edited.

## Bugs

- B1 migration-fabricates-qty | P1 | **confirmed** | `SET quantity = 100.0, buy_price = market_value / 100.0 WHERE quantity IS NULL OR quantity = 0.0` still present, runs unconditionally every invocation (ALTER backfill = 0.0 matches all legacy rows on first run); DB probe: 0 fabricated rows currently survive (see DB section) | add_portfolio_columns.py:49-53 (SET at 51) | backend/migrations/add_portfolio_columns.py | Seed temp sqlite with legacy row (qty backfilled 0.0), run migrate_database, assert qty stays 0/NULL — never 100.
- B2 makedirs-after-create_all | P1 | **confirmed** | `Base.metadata.create_all` at line 40 runs inside `engine.begin()` before `os.makedirs("data")` at 43 (and makedirs never runs if create_all raises); probe: aiosqlite connect to missing dir → `OperationalError: unable to open database file` | app/db/database.py:40 then :43 | backend/app/db/database.py | Point DATABASE_URL at a non-existent temp subdir, call init_db(), assert dir is created and no OperationalError.
- B3 bulk-script-ImportError | P1 | **confirmed** | runtime probe: `ImportError: cannot import name 'create_database_engine' from 'app.db.database'`; only refs are this file's lines 14/26 | test_bulk_operations_integrity.py:14 | backend/test_bulk_operations_integrity.py (delete or rewrite) | `python -c "import test_bulk_operations_integrity"` raises ImportError today; test asserts script removed or importable.
- B4 inverted-rollback-assert | P1 | **confirmed** | `assert final_count >= initial_count, "Transaction rollback failed…"` — passes when count grows (rollback failure), only fails on deletion | test_bulk_operations_integrity.py:175 | backend/test_bulk_operations_integrity.py | If script kept: fixture adds a row inside failing tx, assert `final_count == initial_count` fails under current `>=` logic.
- B5 cleanup-deletes-real-holdings | P1 | **confirmed** | `DELETE … WHERE custom_name LIKE '%Test%' OR ticker IN ('MSFT','GOOGL','TSLA','AAPL')` deletes any live position with those tickers regardless of name | test_bulk_operations_integrity.py:271 | backend/test_bulk_operations_integrity.py | Seed live row ticker=MSFT custom_name='My real money', run cleanup_test_data, assert row still present (currently wiped).
- B6 api-script-pollutes-no-teardown | P1 | **confirmed** | POSTs `/portfolio/bulk_add` MSFT/GOOGL (line 44) and `/portfolio/add` NVDA (line 203); grep of the file: zero DELETE/teardown/cleanup matches | test_api_integrity.py:44, :203 | backend/test_api_integrity.py | Run against seeded DB, assert portfolio row-count unchanged after script (currently +3).
- B7 debug-default-true-sql-echo | P2 | **confirmed** | `debug: bool = Field(default=True, env="DEBUG")` and `echo=settings.debug` both still present → SQL logged by default everywhere | app/config.py:35 + app/db/database.py:20 | backend/app/config.py, backend/app/db/database.py | `Settings().debug is False` with cleared env; engine created with echo off when DEBUG unset.
- B8 allowed_origins-json-only-crash | P2 | **confirmed** | probe with `ALLOWED_ORIGINS="http://a:3000, http://b:3000"` → `SettingsError: error parsing value for field "allowed_origins" from source "EnvSettingsSource"` raised at module-level `settings = Settings()` | app/config.py:39 (crash via :58) | backend/app/config.py | Set comma-separated ALLOWED_ORIGINS, import app.config, assert 2 origins parsed (no exception).
- B9 mutable-shared-default-dict | P2 | **confirmed** | `model_params = Column(JSON, default={})` — same dict object handed to every instance | app/models/database.py:81 | backend/app/models/database.py | Two AnalyticsCache(), mutate `a.model_params`, assert `b.model_params` unaffected.
- B10 nse-nonunique-indexes-select-then-insert | P2 | **confirmed** | non-unique `Index("ix_bhav_symbol_date"…)` :136 (same :155/:177/:199); writer is select-then-insert at india_data_service.py:69-78 and `scalar_one_or_none()` at :111-119 → concurrent dup ingest ⇒ MultipleResultsFound | app/models/database.py:136 (+155,177,199); app/services/india_data_service.py:69-78,111-119 | backend/app/models/database.py, backend/app/services/india_data_service.py | Ingest same bhavcopy date twice concurrently; assert single row / ON CONFLICT, no MultipleResultsFound.
- B11 migration-conn-unbound | P2 | **confirmed** | `conn = sqlite3.connect(...)` at :22 inside `try:` at :20; `finally: if conn:` at :74 → UnboundLocalError if connect raises, masking original | add_portfolio_columns.py:22, :74 | backend/migrations/add_portfolio_columns.py | Monkeypatch sqlite3.connect to raise; assert original exception surfaces, not UnboundLocalError.
- B12 migration-no-backup | P2 | **confirmed** | add_portfolio_columns has no create_backup call before destructive UPDATE; contrast cleanup_duplicates:217 does back up | add_portfolio_columns.py:49 (absence; contrast cleanup…py:217) | backend/migrations/add_portfolio_columns.py | Run migration on seeded temp DB; assert a backup file exists before portfolio_positions mutated.
- B13 cleanup-cwd-relative-path | P2 | **confirmed** | `db_path: str = "data/daisy.db"` + `backup_dir = "data/backups"` CWD-relative (add_portfolio uses Path(__file__) at :12); sqlite3.connect creates empty file in wrong CWD, then `SELECT COUNT(*) FROM stock_timeseries` at :42 crashes leaving stray db | cleanup_duplicates_and_add_constraints.py:17 (crash at :42) | backend/migrations/cleanup_duplicates_and_add_constraints.py | Run from temp CWD without data/; assert no file created + clear error (currently creates empty daisy.db).
- B14 schema-ticker-no-pattern | P2 | **confirmed** | probe: `PortfolioPositionBase(ticker="BAD TICKER!", …)` accepted; pattern exists only in ValidateTickerRequest :165 and portfolio.py `_TICKER_PATTERN` :32 (used at :309/:517, NOT on POST /add which only checks vendor existence at portfolio.py:170) | app/models/schemas.py:13 | backend/app/models/schemas.py | `pytest.raises(ValidationError)` for `PortfolioPositionBase(ticker="BAD TICKER!", …)`.
- B15 api-script-false-passes | P2 | **confirmed** | :135-142 both branches `return True`; :96 accepts HTTP 500 as pass; :275/:284 overall success at 60% threshold | test_api_integrity.py:96, :142, :275 | backend/test_api_integrity.py | If kept: invalid-ticker returning 500 must count as failure; threshold 100%.
- B16 field-env-noop | P3 | **confirmed** | probe: `Field(default=1, env='X')` → `PydanticDeprecatedSince20 … Extra keys: 'env'`, kwarg lands in `json_schema_extra={'env': 'X'}` — no env binding (works only via field-name uppercasing) | app/config.py:14 (pattern all Fields) | backend/app/config.py | Set `DATABASE_URL` env, assert Settings().database_url matches only because name matches; rename field → env override breaks (document/test).
- B17 logger-ignores-log-level | P3 | **confirmed** | `log_level = level or "INFO"` never reads settings; settings.log_level defined only at config.py:36 (grep: zero consumers); uvicorn level hardcoded `"info"` at main.py:178 | app/utils/logger.py:22 | backend/app/utils/logger.py, backend/main.py | Set LOG_LEVEL=WARNING, setup_logger(None), assert logger.level == WARNING.
- B18 init_db-dead-import-comment | P3 | **confirmed** | line 37 comment `# Import all models to ensure they're registered` followed by blank line — no import; models registered only incidentally via main.py:22 router imports | app/db/database.py:37 | backend/app/db/database.py | Call init_db() without importing routers; assert tables still created (or add explicit model import and assert metadata non-empty).
- B19 holdings-except-unmasked | P3 | **confirmed** | `except Exception: masked[ticker] = series` passes raw pre-purchase series through with no log/drop — contradicts module docstring's masking guarantee | app/utils/holdings.py:140-141 | backend/app/utils/holdings.py | Series with broken DatetimeIndex + known holding start: assert ticker is warned/dropped, not silently unmasked.
- B20 holdings-str-slice-date | P3 | **confirmed** | `return str(value)[:10] or None` returns `"abc"`/`"None"` as fake ISO dates; safety relies on downstream pd.Timestamp try/except | app/utils/holdings.py:242 | backend/app/utils/holdings.py | `coerce_holding_date("abc") is None` and `coerce_holding_date("None") is None` (currently truthy junk).

## Improvements

- I1 schemas-redundant-validators | imp | **confirmed** | weight/quantity/buy_price validators :27-43 duplicate `Field(gt=…, le=…)` :14-16; added_on validator :45-49 duplicated at :65-69 | app/models/schemas.py:27 (and :45) | backend/app/models/schemas.py | Behavior-identical validation tests pass after deleting validators (keep Field constraints only).
- I2 schemas-deprecations | imp | **confirmed** | `from pydantic import … validator` :7 (v1 API), `min_items=1` :151 (still enforced — probe ValidationError), `datetime.utcnow` :289 (removed in 3.14) | app/models/schemas.py:7, :151, :289 | backend/app/models/schemas.py | Deprecation-warning-as-error run over schema imports; empty-tickers still rejected after min_items→min_length.
- I3 region-default-us | imp | **confirmed** | `region: str = Field(default="US", …)` in India-first (.NS/.BO) app | app/models/schemas.py:17 | backend/app/models/schemas.py | Position created without region on `*.NS` ticker carries region "IN" (or derived suffix), not "US".
- I4 weak-bounds-target-vol-empty-bulk | imp | **confirmed** | `target_volatility: Optional[float] = 0.15` no gt/lt bounds :264; `positions: List[…]` accepts [] :295 | app/models/schemas.py:264, :295 | backend/app/models/schemas.py | `target_volatility=-1` rejected; `BulkAddRequest(positions=[])` rejected.
- I5 get_db_session-double-close | imp | **confirmed** | `async with SessionLocal()` already closes; `finally: await session.close()` :57 redundant (rollback in except is the only needed bit) | app/db/database.py:50-57 | backend/app/db/database.py | Existing endpoint tests pass after removing explicit close (session still closed once).
- I6 cleanup-alter-add-constraint-unsupported | imp | **confirmed** | probe: `ALTER TABLE t ADD CONSTRAINT uk UNIQUE(a)` → `sqlite3.OperationalError: near "CONSTRAINT": syntax error`; swallowed at :143 with misleading log; real mechanism is CREATE UNIQUE INDEX :125 | cleanup_duplicates_and_add_constraints.py:138-145 | backend/migrations/cleanup_duplicates_and_add_constraints.py | Assert migration log contains no "UNIQUE constraint not added" noise; uniqueness still holds via index.
- I7 cleanup-test-insert-live-table | imp | **confirmed** | validation INSERTs `'TEST'` rows twice into live stock_timeseries :174-183 then SELECT/DELETE :185-189 — correct only if all steps commit together | cleanup_duplicates_and_add_constraints.py:175 | backend/migrations/cleanup_duplicates_and_add_constraints.py | Kill process between INSERT and DELETE; assert no TEST row persists (currently possible), or use temp table.
- I8 backups-untracked-growth | imp | **confirmed** | `backup_dir = "data/backups"` one full copy/run, no retention :19; `git ls-files` shows `backend/data/backups/daisy_backup_20251102_183018.db` tracked; .gitignore `data/*.db` does NOT cover `data/backups/` subdir | cleanup_duplicates_and_add_constraints.py:19 | backend/data/backups/*, backend/.gitignore | `git ls-files backend/data/backups` empty; backups pruned beyond N.
- I9 logger-level-validation-propagate | imp | **confirmed** | `getattr(logging, log_level.upper())` :26/:34 raises AttributeError on typo; no `logger.propagate = False` anywhere in file → double-log with root handlers | app/utils/logger.py:26 | backend/app/utils/logger.py | `setup_logger("x", "TYPO")` doesn't raise (or fails cleanly); no duplicate records with root handler configured.
- I10 diag-script-debris | imp | **confirmed** | `warnings.filterwarnings("ignore")` :2, `import numpy as np` at :23 AFTER `frame()` (:13) that uses np (works only because call is later), bare `asyncio.run(main())` :64 executes on import | _diag_rc.py:2, :23, :64 | backend/_diag_rc.py | `import _diag_rc` has no side effects (currently runs full diagnostic).
- I11 dead-position-fk-relationship | imp | **confirmed** | `position_id` FK :57 appears only at its definition (repo grep); relationship :33 unassigned; `PRAGMA foreign_keys=ON` now active (db/database.py:73) and `db.delete(position)` portfolio.py:720 has no cascade → FK-fail if ever populated | app/models/database.py:33, :57 | backend/app/models/database.py | Populate position_id on a stock row, delete position — assert SET NULL (or drop FK and assert column gone).
- I12 nse-scaffolding-no-writer | imp | **confirmed** | NSEBulkBlockDeal :162 / NSEShareholdingPattern :184 referenced only by own defs + cache purge map (cache_service.py:223,239-240) — schema-without-usage | app/models/database.py:184 (:162) | backend/app/models/database.py | Document as t28/t29 scaffolding (no code change) or add writer test when feature lands.
- I13 test-coverage-gaps | imp | **confirmed** | globs `tests/*config*`, `tests/*migrat*`, `tests/*logger*` → none; grep found no tests importing the two migrations or three root scripts; holdings IS covered (tests/test_holding_aware.py:23-55, test_phase2_disclosure.py:16+); ticker pattern covered at test_contract_p1_batch.py:216-227 | tests/ (absence) | backend/tests/ | New tests exist for Settings env parsing, init_db missing-dir, migration happy-path.
- I14 (audit note) holdings-covered | imp | **confirmed** | test_holding_aware.py imports/ exercises coerce_holding_date, holding_coverage, etc. at :23-55; test_phase2_disclosure.py covers holding_coverage | tests/test_holding_aware.py:23 | — | n/a (already covered).

## Optimizations

- O1 sql-echo-log-tax | opt | **confirmed** | `echo=settings.debug` with debug default True (B7) — every query logged on stdout by default | app/db/database.py:20 | backend/app/db/database.py | Engine created with echo=False when DEBUG unset; log capture shows no SQL lines.
- O2 expires_at-no-index | opt | **confirmed** | `expires_at = Column(...)` :80 with no Index; purge/stats filter on expires_at alone at cache_service.py:129, :161, :168 → full analytics_cache scans | app/models/database.py:80 | backend/app/models/database.py | `EXPLAIN QUERY PLAN` on purge shows USE OF index; or explicit decision documented while table small.
- O3 fetchlogs-unbounded | opt | **confirmed (nuanced)** | insert-only via cache_service.log_fetch_attempt (:98; callers data_service.py:384,400,802,833); only delete is full-wipe in clear-all cache (cache_service.py:236,:247) — no age-based retention anywhere | app/models/database.py:92 (class FetchLog) | backend/app/models/database.py, backend/app/services/cache_service.py | Seed FetchLog older than N days, run retention, assert old rows gone / recent kept.
- O4 duplicate-unique-index-orm-migration-drift | opt | **confirmed** | ORM declares `UniqueConstraint("ticker","date")`+`ix_ticker_date` (database.py:62-63); migration adds 2nd UNIQUE `idx_stock_timeseries_unique_ticker_date` (cleanup…py:125); analytics_cache ORM index NON-unique `ix_ticker_metric` (:85) vs migration UNIQUE `idx_analytics_cache_unique` (:131) → migrated vs fresh-DB schemas differ | migrations/cleanup…py:125, :131 vs models/database.py:62-63, :85 | backend/app/models/database.py, backend/migrations/cleanup_duplicates_and_add_constraints.py | Introspect both a create_all DB and a migrated DB: identical index/constraint sets.
- O5 engine-lifecycle-clean | opt | **confirmed (no issue)** | single module-level engine + async_sessionmaker (db/database.py:18-29), expire_on_commit=False, close_db_connections() teardown; grep found no repeated engine construction | app/db/database.py:18-29 | — | n/a (audit's negative finding stands).

## DB state inspection for migration repair

Read-only probes against `backend/data/daisy.db` (sqlite URI `mode=ro`; DB ~10 MB; WAL files present, wal=0 bytes). **No file modified.**

| Check | Count |
|---|---|
| `portfolio_positions` total rows | **14** |
| rows with `quantity = 100.0` | **0** |
| rows with `quantity = 100.0 AND buy_price = market_value/100.0` | **0** |
| rows with `buy_price = market_value/100.0` (any qty) | **0** |
| rows with `quantity = 0.0 OR quantity IS NULL` (what the WHERE clause targets) | **0** |
| **any fabrication-pattern row (repair candidates)** | **0** |
| distinct quantity values | 13 |

Sample (all look user-typed, none fabricated): `CIPLA.NS qty=2 bp=1431.25 mv=2768`; `ELECTCAST.NS qty=20 bp=104.18`; `MIDCAPIETF.NS qty=190 bp=20.32`; `MCX.NS qty=1 bp=2836.6`; `SELECTIPO.NS qty=40 bp=49.13` (full 14-row dump taken during probe).

Schema signal: `PRAGMA table_info` shows `quantity REAL dflt='0.0'`, `buy_price REAL dflt='0.0'` — server defaults that the ORM model does NOT declare (model uses Python-side `default=0.0` only), i.e. the migration's `ALTER TABLE … DEFAULT 0.0` **did run on this DB at some point**. Yet zero rows match the fabricated pattern, so it most likely ran against an empty/then-replaced table, or all rows were re-imported afterwards.

**Headline for fixer: 0 of 14 rows are fabricated — NO data-repair migration needed for the current DB. The UPDATE at add_portfolio_columns.py:51 is still dangerous (matches any future qty=0/NULL row, including ORM-inserted ones) and must be fixed, but nothing to repair in daisy.db today.**

## Verdict summary

- Confirmed: 40/40 findings (20 bugs, 14 improvements incl. 2 audit negative-notes, 5 optimizations, holdings-coverage note)
- Refuted: 0
- Already-fixed: 0
- Config line drift on cited lines: none (14/35/39/58 exact)
